<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://anish-lakkapragada.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://anish-lakkapragada.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2025-06-06T23:22:03+00:00</updated><id>https://anish-lakkapragada.github.io/feed.xml</id><title type="html">blank</title><subtitle>Anish Lakkapragada&apos;s personal website and blog. </subtitle><entry><title type="html">using the vc dimension &amp;amp; rademacher complexity</title><link href="https://anish-lakkapragada.github.io/blog/2025/vc-rademacher-test/" rel="alternate" type="text/html" title="using the vc dimension &amp;amp; rademacher complexity"/><published>2025-06-03T00:00:00+00:00</published><updated>2025-06-03T00:00:00+00:00</updated><id>https://anish-lakkapragada.github.io/blog/2025/vc-rademacher-test</id><content type="html" xml:base="https://anish-lakkapragada.github.io/blog/2025/vc-rademacher-test/"><![CDATA[ <p><em>TLDR: Testing VC dimension &amp; Rademacher complexity generalization error bounds for a simple perceptron and a rectangular classifier.</em></p> <meta name="viewport" content="width=device-width, initial-scale=1.0"/> <p>Hey all! I hope you are doing well as usual. Over the last week, I realized I’ve never really touched Statistical Learning Theory at all before, despite hearing about it a lot. So I got slightly curious and went through these (IMO very approachable) introductory <a href="https://ocw.mit.edu/courses/15-097-prediction-machine-learning-and-statistics-spring-2012/3f3332b76e8248226fb2285b91cfc6db_MIT15_097S12_lec14.pdf">lecture notes</a> on statistical learning theory by Dr. Cynthia Rudin for MIT’s 15.097.</p> <p>What I found was that the theory yielded these really magical probabilistic bounds on generalization error, whereas the actual computation of these bounds was treated as an afterthought. Specifically, many of these bounds rested on some clever mathematical formalization of the capacity of the function class, such as the VC Dimension or the Rademacher Complexity. In this blog post I hope to explore how to compute these two quantities in actual function class examples. Doing so means that we can concretely test the VC &amp; Rademacher generalization error bounds themselves.</p> <h3 id="establishing-some-preliminaries">establishing some preliminaries</h3> <h4 id="notation">notation</h4> <p><em>Please at least skim this section as notation for statistical learning theory is quite unconsistent across different places.</em></p> <p>We start by defining some notation we’ll need for this blog post, most of which is directly taken from the 15.097 notes. We define our input space as \(\mathcal{X}\) and our output space as \(\mathcal{Y} = \{-1, 1\}\). Our observed data \(\{(X_i, y_i)\}_{i = 1}^m\) are drawn IID from distribution \(D\) on the space \(\mathcal{X} \times \mathcal{Y}\). Our goal is to create a function \(f: \mathcal{X} \to \mathcal{Y}\) that minimizes the true risk \(R^{\text{true}}(f) = \mathbb{P}_{(X, y) \sim D}[f(X) \neq y]\). Often times this risk is uncomputable as \(D\) is unknown and so we must settle by using the empirical risk \(R^{\text{emp}}(f) = \frac{1}{m} \sum_{i = 1}^m \mathbf{1}\{f(X_i) \neq y_i\}\).</p> <p>There’s a vast range of functions we could think of that can map \(\mathcal{X} \to \mathcal{Y}\). As such, we’ll often constrain the empirical risk minimizer function we are searching for to a subset of all those functions, or a function class. Some function classes you are probably familiar with are linear models or 2-layer neural networks.</p> <p>Sometimes, we won’t care about the predictions themselves (i.e. the output of \(f\)), but instead whether they were right or not. For a given function \(f: \mathcal{X} \to \mathcal{Y}\), we can define its loss function \(g: \mathcal{X} \times \mathcal{Y} \to \{0, 1\}\) as the following: \(g(x, y) = \mathbf{1}\{f(x) = y\}\). We can essentially think as the loss function a vehicle to assess the performance of another function (in fact, there is a bijection between \(g\) and \(f\)). For a given function class \(\mathcal{F}\), we can define the loss class \(\mathcal{G}\) as:</p> \[\mathcal{G} = \{[g: (x, y) \to \mathbf{1}\{f(x) = y\}] : f \in \mathcal{F}\}\] <p>which is the set of all loss functions corresponding to all functions in our function class. We can also define the true &amp; empirical risk easily with \(g\):</p> \[P^{\text{true}}(g) = \mathbb{E}_{(X, y) \sim D}[g(X, y)] = R^{\text{true}}(f), \quad P^{\text{emp}}(g) = \frac{1}{m} \sum_{i = 1}^m g(X_i, y_i) = R^{\text{emp}}(f)\] <p>Lastly, note that we define the \(\text{sign}(\cdot)\) function as the following:</p> \[\text{sign}(x) = \begin{cases} 1 &amp; \text{if } x \geq 0 \\ -1 &amp; \text{else} \end{cases}\] <h4 id="our-function-classes">our function classes</h4> <p>To start, we’ll want to actually settle on what function classes we want to play around with. For this blog post, we’ll test two function classes. For each function class, we will test the VC &amp; Rademacher bounds by generating a dataset from some ground-truth reality. We now provide these two function classes, along with the ground-truth parameters we will use for our simulations (boxed):</p> <ol> <li> <p><strong>Linear Threshold Functions: \(\mathcal{F}_1 = \{x \mapsto \text{sign}(\mathbf{w}^Tx) \mid \mathbf{w} \in \mathbb{R}^2\} \quad \boxed{\mathbf{w} = (1, -2)}\)</strong></p> </li> <li> <p><strong>Rectangle Classifiers: \(\mathcal{F}_2 = \{(x_1, x_2) \mapsto \text{sign}(\mathbf{1}\{a \leq x_1 \leq b \} \wedge \mathbf{1}\{c \leq x_2 \leq d \}) \mid (a, b, c, d) \in \mathbb{R}^4 \} \quad \boxed{(a, b, c, d) = (1, 3, 2, 4)}\)</strong></p> </li> </ol> <p>Note that functions in the first function class \(\mathcal{F}_1\) lack a bias term, but are valid functions for our purposes nonetheless. You might also recognize \(\mathcal{F}_1\) to essentially be a class of binary classification perceptrons. The second function class \(\mathcal{F}_2\) is a bit more strange – essentially it posits data in class \(1\) inside a finite 2D rectangle with bottom left corner \((a, c)\) and top right corner \((b, d)\), and data in class \(-1\) as outside this rectangle. One commonality between both these function classes is they are uncountable<sup id="fnref:countable"><a href="#fn:countable" class="footnote" rel="footnote" role="doc-noteref">1</a></sup>, and thus well-suited for VC &amp; Rademacher bounds.</p> <p>While the VC &amp; Rademacher Bound apply for any function in our function class \(\mathcal{F}_k\), we’ll be most interested in the function \(f_n = \underset{f \in \mathcal{F}_k}{\text{argmin}} \ R^{\text{emp}}(f) \in \mathcal{F}_k\), or the minimizer of the empirical risk. This is because \(f_n\) is the actual function in our function class we would use. It would be helpful then to understand how we actually would learn or at least estimate \(f_n\) for our two function classes. We detail the procedures below for each function class:</p> <p><strong>1. Learning \(\mathbf{w} \in \mathbb{R}^2\) for \(\mathcal{F}_1\)</strong></p> <p>To learn the weights for this perceptron, we’ll use the … perceptron algorithm, which is very simple. Suppose we have dataset \(\{(X_i, y_i)\}_{i = 1}^m\). We first start by setting \(\mathbf{w} \leftarrow \mathbf{0} \in \mathbb{R}^2\). Note that our goal is to ensure that \(\forall 1 \leq i \leq m, \ y_i\mathbf{w}^TX_i \geq 0\), as this indicates a correct classification. Thus, at every iteration of this algorithm, if for our \(i\)th sample \(y_i\mathbf{w}^TX_i &lt; 0\), then we will set<sup id="fnref:note"><a href="#fn:note" class="footnote" rel="footnote" role="doc-noteref">2</a></sup> \(\mathbf{w} \leftarrow \mathbf{w} + y_iX_i\).</p> <p>This algorithm is so simple in code:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">learn_perceptron</span><span class="p">(</span><span class="n">X</span><span class="p">,</span> <span class="n">y</span><span class="p">,</span> <span class="n">iters</span><span class="p">):</span>
    <span class="n">w</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">zeros</span><span class="p">(</span><span class="mi">2</span><span class="p">)</span> <span class="c1"># initialize weights
</span>    <span class="k">for</span> <span class="n">_</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">iters</span><span class="p">):</span>
        <span class="k">for</span> <span class="n">xi</span><span class="p">,</span> <span class="n">yi</span> <span class="ow">in</span> <span class="nf">zip</span><span class="p">(</span><span class="n">X</span><span class="p">,</span> <span class="n">y</span><span class="p">):</span>
            <span class="k">if</span> <span class="n">yi</span> <span class="o">*</span> <span class="n">np</span><span class="p">.</span><span class="nf">dot</span><span class="p">(</span><span class="n">w</span><span class="p">,</span> <span class="n">xi</span><span class="p">)</span> <span class="o">&lt;=</span> <span class="mi">0</span><span class="p">:</span> <span class="c1"># wrong 
</span>                <span class="n">w</span> <span class="o">+=</span> <span class="n">yi</span> <span class="o">*</span> <span class="n">xi</span> <span class="c1"># update
</span>    <span class="k">return</span> <span class="n">w</span>
</code></pre></div></div> <p><strong>2. Learning \((a, b, c, d)\) for \(\mathcal{F}_2\)</strong></p> <p>Using the assumption that most of our data’s class \(1\) observations can be fit into a finite 2D rectangle, we can simply choose \((a, b, c, d)\) by looking at the maximum and minimum x &amp; y-coordinates of observations in class \(1\). In practice we won’t use the maximum &amp; minimum, but instead the 95th &amp; 5th percentile. Defining some dataset \(\{((h_i, v_i), y_i)\}_{i = 1}^m\) for this task<sup id="fnref:notation"><a href="#fn:notation" class="footnote" rel="footnote" role="doc-noteref">3</a></sup>, we estimate our parameters as the following:</p> \[\hat{a} = \text{perc}_{0.05}(\{ h_i: y_i = 1 \}) \quad \hat{b} = \text{perc}_{0.95}(\{ h_i: y_i = 1 \}) \quad \hat{c} = \text{perc}_{0.05}(\{ v_i: y_i = 1 \}) \quad \hat{d} = \text{perc}_{0.95}(\{ v_i: y_i = 1 \})\] <p>We are now ready to measure our function class’s capacity.</p> <h3 id="understanding-our-function-classs-vc-dimension">understanding our function class’s vc dimension</h3> <p><em>For a formal definition and construction of the VC Dimension, please see these <a href="https://engineering.purdue.edu/ChanGroup/ECE595/files/Lecture27_vc.pdf">lecture slides</a>. I do not define it here.</em></p> <p>Actually proving the VC Dimension for a function class can be quite tricky, as to conclude that a function class has VC dimension \(h\) one must show that there exists a set of \(h\) points that our function class can shatter and that <em>no</em> set of \(h + 1\) points exists that can be shatterable. The good news is that both of our defined function classes have established VC dimensions. For our first function class, the VC Dimension is two (as we are in \(\mathbb{R}^2\)), and for our second function class the VC Dimension is four.</p> <p>While deriving the VC Dimension for our perceptron is quite hard (<a href="https://engineering.purdue.edu/ChanGroup/ECE595/files/Lecture27_vc.pdf">done here</a> if our perceptron had a bias), understanding the VC Dimension for our rectangular class is slightly easier. See the next paragraph if interested.</p> <p><em>Consider any four points in the plane - for every prediction configuration<sup id="fnref:note2"><a href="#fn:note2" class="footnote" rel="footnote" role="doc-noteref">4</a></sup>, we can construct a rectangle such that only points of class \(1\) are inside. To conclude that the VC Dimension is four, though, we must show that it is not five. This can be seen from the fact that for any 5 points, we can intuitively expect one of those points to be the “interior”. Then setting the other four as all class \(1\) and this “interior” point as class \(-1\), no such rectangle can be made to accurately capture this prediction configuration without labeling the interior point as class \(1\). Thus the number of dichotomies made by \(\mathcal{F}_2\) on any five points is always \(&lt; 2^5 \implies\) the growth function \(\neq 2^5 \implies\) the VC Dimension is \(&lt; 5\). This concludes our extremely informal proof that the VC Dimension of \(\mathcal{F}_2\) is four.</em></p> <p>We now present the VC Bound on generalization error, which we will actually test later. Suppose we have a function class \(\mathcal{F}\) with VC Dimension \(h\), and our data follows a distribution \(D\). Under this setup, we will draw training data \(\mathbf{Z} = \{(X_1, Y_1), \dots, (X_m, Y_m) \}\) from distribution \(D^m\), where \(m \geq h\). Then the VC Bound says \(\forall \delta &gt; 0\):</p> <p>\begin{equation} \mathbb{P}_{\mathbf{Z} \sim D^m}[\forall f \in \mathcal{F} : R^{\text{true}}(f) \leq R^{\text{emp}}(f) + 2\sqrt{2 \frac{h \log \frac{2em}{h} + \log \frac{4}{\delta}}{m}}] \geq 1 - \delta \label{vc-bound} \end{equation}</p> <p>Note that the probability here stems from the randomnes of our training data, which will affect the empirical risk quantity.</p> <h3 id="estimating-the-rademacher-complexity">estimating the rademacher complexity</h3> <p><em>For a very thorough treatment of Rademacher Complexity, way above my paygrade, see these <a href="https://www.cs.cmu.edu/~ninamf/ML11/lect1117.pdf">lecture notes</a>.</em></p> <p>We now move to another approach to measuring the capacity of a function class, Rademacher Complexity. This approach is <em>distribution dependent</em>, and so it will focus on data from our actual distribution. This is in contrast to VC Dimension, which only concerned itself with the existence of <em>any</em> possible points being shatterable. Defining the Rademacher Complexity is a two-step process. Suppose we have a fixed dataset \(\mathbf{Z} = \{(X_i, Y_i)\}_{i = 1}^m \overset{\text{IID}}{\sim} D^m\) of \(m\) points, where \(D\) is our data distribution. We first give the <em>Empirical Rademacher Complexity</em> $\mathcal{\hat{R}_m(F)}$ of function class \(\mathcal{F}\):</p> \[\mathcal{\hat{R}_m(F)} = \mathbb{E}_{\mathbf{\sigma}}[\underset{f \in \mathcal{F}}{\sup} \frac{1}{m} \sum_{i = 1}^m \sigma_i f(X_i)]\] <p>where \(\sigma_1, \dots, \sigma_m\) are IID <a href="https://en.wikipedia.org/wiki/Rademacher_distribution">Rademacher</a> random variables. Then, the Rademacher complexity \(\mathcal{R_m(F)}\) of \(\mathcal{F}\) is given by another expectation:</p> \[\mathcal{R_m(F)} = \mathbb{E}_{\mathbf{Z} \sim D^m}[\mathcal{\hat{R}_m(F)}] = \mathbb{E}_{\mathbf{Z} \sim D^m}[\mathbb{E}_{\mathbf{\sigma}}[\underset{f \in \mathcal{F}}{\sup} \frac{1}{m} \sum_{i = 1}^m \sigma_i f(X_i)]]\] <p>This expectation is over \(\mathbf{Z} \sim D^m\), or the \(m\)-sized dataset draws. In brief, the main idea of Rademacher complexity is that function classes which can fit random noise better have more capacity. Before explaining how we will actually estimate this \(\mathcal{R_m(F)}\) quantity, we’ll quickly present why we care about it (i.e. the Rademacher bound<sup id="fnref:bound-note"><a href="#fn:bound-note" class="footnote" rel="footnote" role="doc-noteref">5</a></sup>):</p> \[\forall \delta &gt; 0, \mathbb{P}_{\mathbf{Z} \sim D^m}[\forall f \in \mathcal{F} : R^{\text{true}}(f) \leq R^{\text{emp}}(f) + \mathcal{R_m(f)} + \sqrt{\frac{\log \frac{1}{\delta}}{2m}}] \geq 1 - \delta\] <p>Back to now estimating this Rademacher complexity term. The first thought might be a grid sampling procedure (i.e. first sample \(\mathbf{Z}\) and then \(\mathbf{\sigma}\)) and then try to compute the innermost \(\underset{f \in \mathcal{F}}{\sup} \frac{1}{m} \sum_{i = 1}^m \sigma_i f(X_i)\) term (more on that later.) While this is not a bad idea, I’m just a bit wary about how many iterations or trials it would take. Instead, we can cheat a little and just estimate the Empirical Rademacher Complexity Term. This is because we have a really nice generalization error bound involving just the empirical complexity:</p> <p>\begin{equation} \forall \delta &gt; 0, \mathbb{P}_{\mathbf{Z} \sim D^n}[\forall f \in \mathcal{F} : R^{\text{true}}(f) \leq R^{\text{emp}}(f) + \mathcal{\hat{R}_m(F)} + 3\sqrt{\frac{\log \frac{2}{\delta}}{m}}] \geq 1 - \delta \label{emp-rad-bound} \end{equation}</p> <details> <summary> Justification of above generalization error bound </summary> We first start with this bound taken from <a href="https://www.cs.cmu.edu/~ninamf/ML11/lect1117.pdf">here</a> (Section 1.3.2, Theorem 2) that utilizes the empirical complexity, albeit now in terms of the loss function itself (refer to earlier notation section): $$ \forall \delta &gt; 0, \mathbb{P}_{\mathbf{Z} \sim D^n}[\forall g \in \mathcal{G} : P^{\text{true}}(g) \leq P^{\text{emp}}(g) + 2\mathcal{\hat{R}_m(G)} + 3\sqrt{\frac{\log \frac{2}{\delta}}{m}}] \geq 1 - \delta $$ Note that $ P^{\text{true}}(g) = R^{\text{true}}(f)$ (and same for the empirical risk). We now relate the Empirical Rademacher Complexity of the loss class to the function class (using a nearly identical derivation as found in the 15.097 notes): $$ \small \mathcal{\hat{R}_m(G)} = \mathbb{E}_{\mathbf{\sigma}}[\underset{g \in \mathcal{G}}{\sup} \frac{1}{m} \sum_{i = 1}^m \sigma_i \mathbf{1} \{f(X_i) \neq y_i \}] = \mathbb{E}_{\mathbf{\sigma}}[\underset{f \in \mathcal{F}}{\sup} \frac{1}{m} \sum_{i = 1}^m \sigma_i \frac{1}{2} (1 - Y_i f(X_i))] = \frac{1}{2}\mathbb{E}_{\mathbf{\sigma}}[\underset{f \in \mathcal{F}}{\sup} \frac{1}{m} \sum_{i = 1}^m \sigma_i Y_if(X_i)] = \frac{1}{2}\mathbb{E}_{\mathbf{\sigma}}[\underset{f \in \mathcal{F}}{\sup} \frac{1}{m} \sum_{i = 1}^m \sigma_i f(X_i)] = \frac{1}{2} \mathcal{\hat{R}_m(F)} $$ The main tricks here are that $\sigma_iY_i$ and $-\sigma_i$ are still Rademacher variables (so they can just be expressed as just $\sigma_i$), and also that $\mathbb{E}_{\mathbf{\sigma}}[\underset{f \in \mathcal{F}}{\sup} \frac{1}{m} \sum_{i = 1}^m \sigma_i]= \mathbb{E}_{\mathbf{\sigma}}[\frac{1}{m} \sum_{i = 1}^m \sigma_i] = 0$. Substituting $\frac{1}{2} \mathcal{\hat{R}_m(F)}$ into our above bound (along with the empirical risks), we arrive at \eqref{emp-rad-bound}. </details> <p><br/></p> <p>Estimating \(\mathcal{\hat{R}_m(F)}\) is not nearly as daunting. Given some random realization of \(m\) data points \(\{X_i\}_{i = 1}^m\), all we have to do is repeatedly across \(T\) trials sample Rademacher variables \(\sigma_1, \dots, \sigma_m\). Then for each trial, we train a model in our function class with our original data with corresponding labels \(\sigma_1, \dots, \sigma_m\). For this trained function \(f^*\), the summation \(\frac{1}{m} \sum_{i = 1}^m \sigma_i f^*(X_i) = \underset{f \in \mathcal{F}}{\sup} \frac{1}{m} \sum_{i = 1}^m \sigma_i f(X_i)\). Averaging these \(\frac{1}{m} \sum_{i = 1}^m \sigma_i f^*(X_i)\) quantities across each of our \(T\) trials will lead to an estimate for \(\mathbb{E}_{\mathbf{\sigma}}[\underset{f \in \mathcal{F}}{\sup} \frac{1}{m} \sum_{i = 1}^m \sigma_i f(X_i)] = \mathcal{\hat{R}_m(F)}\).</p> <p>So there we have it! A way to estimate (for any function class!) the (empirical) Rademacher complexity.</p> <h3 id="testing-out-our-bounds">testing out our bounds!</h3> <p>Now that we have generalization error bounds \eqref{vc-bound} and \eqref{emp-rad-bound}, let’s actually test them for our function classes.</p> <h5 id="mathcalf_1-binary-classification-perceptron">\(\mathcal{F}_1\): binary classification perceptron</h5> <p>We train our perceptron on \(m_{\text{train}} = 10^4\) data points. While \(R^{\text{true}}(f)\) is pretty much unknowable, we’ll still want to estimate it. To do so, we approximate \(R^{\text{true}}(f)\) by evaluating our trained model on a test set of \(m_{\text{test}} = 10^4\) data points. We call this approximation \(R^{\text{test}}(f)\).</p> <p>The below plot shows, at each iteration, \(R^{\text{test}}(f)\) and the 95% VC &amp; Rademacher Bounds<sup id="fnref:rad-estimation"><a href="#fn:rad-estimation" class="footnote" rel="footnote" role="doc-noteref">6</a></sup> on \(R^{\text{true}}(f)\):</p> <div style="text-align: center;"> <img src="/assets/img/vc-rademacher-post/vc-rad-bounds-f1.png" style="max-width: 90%; height: auto;"/> </div> <p>Right off the bat, we can see that although no bound appears particularly tight when compared to our estimate of \(R^{\text{true}}(f)\) (green curve), the VC Bound appears to be more than two times more conservative than the Rademacher bound. The unfortunate reality is that while the VC &amp; Rademacher bounds provide formal justifications for the relationships between model complexity and overfitting, they are often too useless to be actually meaningful. The reason that we had to set \(m_{\text{train}}\) so high is that for datasets on the size of \(10^2\), the VC Bound was $&gt;1$, which is completely useless as \(R^{\text{true}}(f) \leq 1\).</p> <h5 id="mathcalf_2-rectangular-classifier">\(\mathcal{F}_2\): rectangular classifier</h5> <p>This function class is slightly different than the first as it is not iteratively learned. Unfortunately, that means we won’t have any pretty plots. We proceed with the same setup of \(m_{\text{train}} = m_{\text{test}} = 10^4\) and report the bounds:</p> <ul> <li>\(R^{\text{test}}(f)\) estimate of \(R^{\text{true}}(f)\): <strong>0.0071</strong></li> <li>95% Rademacher Bound on \(R^{\text{true}}(f)\): <strong>0.0640</strong></li> <li>95% VC Bound On \(R^{\text{true}}(f)\): <strong>0.1903</strong></li> </ul> <p>Once again, we see that our bounds are still too conservative, with the VC bound being more than 3x the Rademacher bound.</p> <h3 id="code">code</h3> <p>The code for the two function classes can be found <a href="https://github.com/anish-lakkapragada/vc-rademacher-test">here</a>.</p> <h3 id="resources">resources</h3> <p>This blog post drew from a good amount of sources. I highly recommend the 15.097 notes as a place to start understanding Statistical Learning Theory. After that, here are some further places to go:</p> <ol> <li> <p>For understanding the VC dimension of our two function classes, see these <a href="https://engineering.purdue.edu/ChanGroup/ECE595/files/Lecture27_vc.pdf">slides</a> from Purdue’s ECE 595.</p> </li> <li> <p>For understanding the Rademacher complexity bounds (and seeing their derivations), feel free to take a look at the <a href="https://www.jmlr.org/papers/volume3/bartlett02a/bartlett02a.pdf">original paper</a> presenting these bounds (Bartlett &amp; Mendleson 2002) or these <a href="https://www.cs.cmu.edu/~ninamf/ML11/lect1117.pdf">lecture notes</a> from CMU’s 8803. Most of the notation I used when presenting Rademacher complexity was from the latter.</p> </li> </ol> <p><em>Thank you for reading this blog post. If you have any questions or noticed any errata, please <a href="mailto:anish.lakkapragada@yale.edu">email me</a>.</em></p> <div class="footnotes" role="doc-endnotes"> <ol> <li id="fn:countable"> <p>We could have easily made these function classes countable by defining all parameters \(\in \mathbb{N}\). For countable or even finite function classes, using Ockham’s Razor Bound may be more suitable. See handy derivations in <a href="/notes/">notes</a> for two-sided Ockham’s Razor Bound in finite function class case. <a href="#fnref:countable" class="reversefootnote" role="doc-backlink">&#8617;</a></p> </li> <li id="fn:note"> <p>Updating \(\mathbf{w}\) this way essentially either pushes \(\mathbf{w}\) towards (if \(y_i = 1\)) or away (if \(y_i = -1\)) from \(X_i\), so as to increase \(y_i\mathbf{w}^TX_i\). <a href="#fnref:note" class="reversefootnote" role="doc-backlink">&#8617;</a></p> </li> <li id="fn:notation"> <p>Sorry for the weird notation, but had to find a way to get around confusing of the \(y\)-coordinate for the \(i\)th point and its label \(y_i\). <a href="#fnref:notation" class="reversefootnote" role="doc-backlink">&#8617;</a></p> </li> <li id="fn:note2"> <p>There is a meaningful distinction between my use of “prediction configuration” and dichotomy here. A dichotomy is a labeling of a set of points by a function in our function class (i.e. \(\{f(z_1), \dots, f(z_m)\}\) for data points \(\{z_1, \dots, z_m\}\) and function \(f \in \mathcal{F}\).) A <em>prediction configuration</em> is a pre-specified labeling \(\underbrace{\{\pm 1, \dots, \pm 1\}}_{m \text{ entries}}\) for our points. We say a function class “shatters” a set \(\{z_1, \dots, z_m\}\) when it can generate \(2^m\) unique dichotomies, or the number of possible prediction configurations. <a href="#fnref:note2" class="reversefootnote" role="doc-backlink">&#8617;</a></p> </li> <li id="fn:bound-note"> <p>The below bound is taken directly from Theorem 5(b) in the seminal <a href="https://www.jmlr.org/papers/volume3/bartlett02a/bartlett02a.pdf">Bartlett &amp; Mendelson 2002</a> paper. Note that the way \(\mathcal{R}_n(F)\) is defined in the paper is double the way it is defined here, hence the discrepancy in constants. <a href="#fnref:bound-note" class="reversefootnote" role="doc-backlink">&#8617;</a></p> </li> <li id="fn:rad-estimation"> <p>We estimated the Rademacher Complexity with \(T = 100\) trials. <a href="#fnref:rad-estimation" class="reversefootnote" role="doc-backlink">&#8617;</a></p> </li> </ol> </div>]]></content><author><name></name></author><category term="statistics, ml"/><category term="statistical-learning-theory"/><summary type="html"><![CDATA[just some basic statistical learning *practice*]]></summary></entry><entry><title type="html">exponential family discriminant analysis</title><link href="https://anish-lakkapragada.github.io/blog/2025/efda/" rel="alternate" type="text/html" title="exponential family discriminant analysis"/><published>2025-05-18T00:00:00+00:00</published><updated>2025-05-18T00:00:00+00:00</updated><id>https://anish-lakkapragada.github.io/blog/2025/efda</id><content type="html" xml:base="https://anish-lakkapragada.github.io/blog/2025/efda/"><![CDATA[<p><strong>I have polished this blog post with some more detail in <a href="/notes/efda.pdf">this PDF</a>.</strong></p> <p><em>TLDR: Generalizing the Linear Discriminant Analysis (LDA) procedure to settings in which data in each class are all distributed according to the same exponential family distribution. Then testing this procedure with one such distribution, the Weibull.</em></p> <meta name="viewport" content="width=device-width, initial-scale=1.0"/> <p>Hey all! Hope you are doing well as usual. I am hoping in this somewhat more involved blog post to</p> <ul> <li>(1) give a brief overview of Linear Discriminant Analysis (LDA) and its relation to Logistic Regression</li> <li>(2) generalize LDA to settings in which our data is distributed according to an exponential family distribution</li> <li>(3) test our generalization in (2) with some toy datasets.</li> </ul> <p>We start with an introduction to LDA.</p> <h3 id="a-primer-on-linear-discriminant-analysis">a primer on linear discriminant analysis</h3> <p><em>Note: While LDA and Logistic Regression can be extended to be used for multiple classes, we focus solely on binary classification in this post.</em></p> <p>Linear Discriminant Analysis (LDA) is a classification technique which models the distribution of data conditional on its class (i.e. \(X \mid Y\)), so as to create a (Bayes) classifier that models \(Y \mid X\). First note that this is in contrast to Logistic Regression, which directly models \(Y \mid X\). Let us establish some preliminaries: supppose we have covariates \(X = (x_1, \dots, x_p) \in \mathbb{R}^p\) and binary response \(Y \in \{0, 1\}\). Our end goal is to model \(p(X) = \mathbb{P}[Y = 1 \mid X]\), as it is well known that a classifier \(h\):</p> \[h(x) = \begin{cases} 1 &amp; \text{if } p(X) \geq 0.5 \\ 0 &amp; \text{else} \end{cases}\] <p>is a Bayes Classifier<sup id="fnref:bayes-classifier-note"><a href="#fn:bayes-classifier-note" class="footnote" rel="footnote" role="doc-noteref">1</a></sup>. For logistic regression, \(p(X)\) has the following form:</p> \[p(X) = \mathbb{P}[Y = 1 \mid X] = \frac{\text{exp}(\beta_0 + \sum_{i = 1}^p \beta_i X_i)}{1 + \exp(\beta_0 + \sum_{i = 1}^p \beta_i X_i)}\] <p>for learnable parameters \(\beta \in \mathbb{R}^{p + 1}\). For LDA, however, this is done through Bayes Rule. Let us define \(f_0(x)\) and \(f_1(x)\) as the PDFs of \(X \mid Y = 0\) and \(X \mid Y = 1\) respectively, and \(\alpha = \mathbb{P}[Y = 1]\), or the marginal probability of \(Y = 1\) across all data. LDA then gives the following form for \(p(X)\):</p> <p>\begin{equation} p(X) = \mathbb{P}[Y = 1 \mid X] = \frac{\mathbb{P}[X \mid Y = 1] \mathbb{P}(Y = 1)}{\mathbb{P}[X \mid Y = 1] \mathbb{P}(Y = 1) + \mathbb{P}[X \mid Y = 0] \mathbb{P}(Y = 0)} = \frac{\alpha f_1(x)}{\alpha f_1(x) + (1 - \alpha) f_0(x)} \label{p-x} \end{equation}</p> <p>Thus, LDA’s job is to learn \(f_0(x), f_1(x),\) and \(\alpha\) so as to model \(p(X)\). To do so, it assumes that \(X \in \mathbb{R}^p\) has a normal distribution for each class with a shared covariance matrix \(\mathbf{\Sigma} \in \mathbb{R}^{p \times p}\). Restated, LDA posits:</p> \[X \mid Y = 1 \sim \mathcal{N}(\mathbf{\mu_{1}}, \mathbf{\Sigma}), \quad X \mid Y = 0 \sim \mathcal{N}(\mathbf{\mu_{0}}, \mathbf{\Sigma})\] <p>Thus to model \(f_0(x)\) and \(f_1(x)\), LDA learns parameters \(\mathbf{\mu_{1}}, \mathbf{\mu_{0}}, \mathbf{\Sigma}\) (and \(\alpha\)) through MLE on the given dataset \(\mathcal{D} = \{ (X_i, Y_i) \}_{i = 1}^{n}\). The MLEs are given below:</p> \[\hat{\alpha} = \frac{N_1}{n}, \quad \quad \underset{k \in \{0, 1\}}{\mathbf{\hat{\mu}_k}} = \frac{1}{N_k} \sum_{i : Y_i = k} X_i, \quad \quad \hat{\mathbf{\Sigma}} = \frac{1}{n} \sum_{k = 0}^1 \sum_{i: Y_i = k} (X_i - \mathbf{\hat{\mu}_k})(X_i - \mathbf{\hat{\mu}_k})^T\] <p>where \(N_1\) and \(N_0\) are the number of observations in \(\mathcal{D}\) belonging to class one and zero respectively.</p> <p>We conclude this section with providing some understanding of the commonalities and differences between logistic regression and LDA. We start by giving the log-odds ratio of the LDA:</p> \[\begin{equation} \log \frac{\mathbb{P}[Y = 1 \mid X]}{\mathbb{P}[Y = 0 \mid X]} = \log \frac{\alpha f_1(x)}{(1 - \alpha) f_0(x)} = \log \frac{\alpha}{1 - \alpha} + \log \frac{\exp(-\frac{1}{2} (x - \mathbf{\mu_{1}})^T \Sigma^{-1} (x - \mathbf{\mu_{1}}) )}{\exp(-\frac{1}{2} (x - \mathbf{\mu_{0}})^T \Sigma^{-1} (x - \mathbf{\mu_{0}}) )} \label{log-odds} \end{equation}\] \[= \underbrace{\log \frac{\alpha}{1- \alpha} - \frac{1}{2} \mathbf{\mu_{1}}^T \mathbf{\Sigma}^{-1} \mathbf{\mu_{1}} + \mathbf{\mu_{0}}^T \mathbf{\Sigma}^{-1} \mathbf{\mu_{0}}}_{\beta_0} + \sum_{i = 1}^p \underbrace{[\mathbf{\Sigma}^{-1} (\mathbf{\mu_{1}} - \mathbf{\mu_{0}})]_i}_{\beta_i} \cdot X_i\] <p>We can see that the log-odds ratios in LDA are modeled as a linear function of \(x\) (hence the name <em>Linear</em> Discriminant Analysis<sup id="fnref:qda"><a href="#fn:qda" class="footnote" rel="footnote" role="doc-noteref">2</a></sup>). Furthermore, these coefficients in LDA can be thought of as parametrized versions of the logistic regression parameters \(\beta\).</p> <p>In a nutshell, LDA offers a clean closed-form solution in exchange for strong parametric assumptions. Logistic regression gives us the opposite.</p> <h3 id="exponential-family-discriminant-analysis">exponential family discriminant analysis</h3> <p>The achilles heel of the LDA is that it requires the strong assumption that observations in each class follow a normal distribution with shared covariance. So what if we could perform the LDA procedure but for all sorts of distributions? In this blog post, we aim to do exactly that for distributions falling under an exponential family.</p> <h5 id="exponential-family-an-abbreviated-introduction">exponential family: an abbreviated introduction</h5> <p>An <em>exponential family</em> is a set of probability distributions whose PDF \(f(\mathbf{x} \mid \mathbf{\eta})\) can be expressed as (using multivariable notation):</p> \[f(\mathbf{x} \mid \mathbf{\eta}) = h(\mathbf{x}) \exp(\mathbf{\eta} \cdot T(\mathbf{x}) - A(\mathbf{\eta}))\] <p>Before we explain the meaning of these functions in this seemingly strange PDF, it is important to stress that many common distributions we work with (e.g. $\mathcal{N}$, $\chi^2$, $\Gamma$) all fall under the exponential family. We now discuss two of these functions in our exponential family PDF:</p> <ul> <li> <p>\(T(\mathbf{x}) \in \mathbb{R}^d\) (for some \(d \in \mathbb{N}\)) is the <em>sufficient statistic</em>. In words, this means that \(T(\mathbf{x})\) holds all information about parameter $\mathbf{\theta}$. In math, the conditional distribution \(\mathbf{x} \mid T(\mathbf{x})\) does not depend at all on $\mathbf{\theta}$.</p> </li> <li> <p>\(\mathbf{\eta} \in \mathbb{R}^d\) is the natural parameter. For our purposes, it is a convenient re-expression of \(\mathbf{\theta}\) amenable to this form (meaning that we have some (ideally invertible) function \(\mathbf{\theta} \mapsto \mathbf{\eta}\)).</p> </li> </ul> <p>The remaining functions \(A(\mathbf{\eta}) \in \mathbb{R}\) and \(h(\mathbf{x}) \in \mathbb{R}\) are not too important for our purposes other than to just make sure that \(f(\mathbf{x} \mid \mathbf{\eta})\) is a valid PDF (the former can be thought of as a normalization constant). <em>There is a lot more to the exponential family. Feel free to read <a href="https://en.wikipedia.org/wiki/Exponential_family">the Wikipedia page</a> and these <a href="https://people.eecs.berkeley.edu/~jordan/courses/260-spring10/other-readings/chapter8.pdf">advanced notes</a> I found for a much more thorough discussion.</em></p> <h5 id="discriminant-analysis-for-exponential-family-data-distributions">discriminant analysis for exponential family data distributions</h5> <p>Having established all background requisite knowledge, we are ready to understand how we can generalize LDA to data with class-conditional distributions in the exponential family. With a slight abuse of terminology<sup id="fnref:why"><a href="#fn:why" class="footnote" rel="footnote" role="doc-noteref">3</a></sup>, let us call this new procedure we are outlining as exponential family discriminant analysis (EFDA).</p> <p>We start with our assumptions. We assume we have two classes of data where data in class one and zero are distributed according to respective exponential family PDFs \(f_1(\mathbf{x} \mid \mathbf{\eta}_1)\) and \(f_0(\mathbf{x} \mid \mathbf{\eta}_0)\) such that:</p> \[\underset{k \in \{0, 1\}}{f_k(\mathbf{x} \mid \mathbf{\eta}_k)} = h(\mathbf{x}) \exp[\mathbf{\eta}_k \cdot T(\mathbf{x}) - A(\mathbf{\eta}_k)]\] <p>First note that PDFs \(f_1\) and \(f_0\) only differ by their true (natural) parameter values \(\mathbf{\eta}_1\) and \(\mathbf{\eta}_0\). To perform classification for EFDA, we aim to model \(p(X)\) in the same way we did for LDA (see \eqref{p-x}). Thus, we must estimate through MLE the parameters \(\alpha, \mathbf{\eta}_1,\) and \(\mathbf{\eta}_0\). We now derive these MLEs, starting with our log-likelihood function:</p> \[\mathcal{L}(\alpha, \mathbf{\eta}_0, \mathbf{\eta}_1) = \sum_{i = 1}^n \log f(X_i, Y_i) = \sum_{i = 1}^n \mathbf{1}\{Y_i = 1\} \log[\mathbb{P}(X_i \mid Y_i = 1) \mathbb{P}(Y_i = 1)] + \mathbf{1}\{Y_i = 0\} \log[\mathbb{P}(X_i \mid Y_i = 0) \mathbb{P}(Y_i = 0)]\] \[= \sum_{i = 1}^n \log[h(X_i)] + \mathbf{1}\{Y_i = 1\}[\log(\alpha) + \mathbf{\eta}_1 \cdot T(X_i) - A(\mathbf{\eta}_1)] + \mathbf{1}\{Y_i = 0\}[\log(1 - \alpha) + \mathbf{\eta}_0 \cdot T(X_i) - A(\mathbf{\eta}_0)]\] <p>From the derivations below, you can see that the MLEs for $\mathbf{\eta}_1$ and $\mathbf{\eta}_0$ do not have a general closed-form solution. This is because the function $A(\mathbf{\eta})$ will differ among distributions in the exponential family. For example, $A(\mathbf{\eta}) = e^{\mathbf{\eta}}$ for the Poisson distribution but $A(\mathbf{\eta}) = \log(1 + e^{\mathbf{\eta}})$ for the Bernoulli distribution. As such, the best we can do is give a condition for these MLEs, which we present below (boxed):</p> <details> <summary> Derivation of $\hat{\alpha}$ </summary> $$ 0 = \frac{\partial \mathcal{L}}{\partial \alpha} = \sum_{i = 1}^n \frac{\mathbf{1}\{Y_i = 1 \}}{\alpha} - \frac{\mathbf{1}\{Y_i = 0\}}{1 - \alpha} \implies (1 - \alpha) \sum_{i = 1}^n \mathbf{1}\{Y_i = 1 \} = \alpha \sum_{i = 1}^n \mathbf{1}\{Y_i= 0\} \implies \hat{\alpha} = \frac{N_1}{n} $$ </details> <details> <summary> Derivation of $\hat{\mathbf{\eta}}_1$ and $\hat{\mathbf{\eta}}_0$ </summary> $$ 0 = \frac{\partial \mathcal{L}}{\partial \mathbf{\eta}_1} = \sum_{i = 1}^n \mathbf{1}\{Y_i = 1\} [T(X_i) - \frac{\partial A(\mathbf{\eta}_1)}{\partial \mathbf{\eta}_1}] \implies \sum_{i: Y_i = 1}^n T(X_i) = N_1 \cdot \frac{\partial A(\mathbf{\eta}_1)}{\partial \mathbf{\eta}_1} $$ and by identical logic our MLE for $\mathbf{\eta}_0$ must satisfy: $$\sum_{i: Y_i = 0}^n T(X_i) = N_0 \cdot \frac{\partial A(\mathbf{\eta}_0)}{\partial \mathbf{\eta}_0}$$ </details> <p><br/></p> \[\begin{equation} \boxed{ \hat{\alpha} = \frac{N_1}{n}, \quad \sum_{i: Y_i = 1}^n T(X_i) = N_1 \cdot \frac{\partial A(\mathbf{\eta}_1)}{\partial \mathbf{\eta}_1}, \quad \sum_{i: Y_i = 0}^n T(X_i) = N_0 \cdot \frac{\partial A(\mathbf{\eta}_0)}{\partial \mathbf{\eta}_0}} \label{efda-mle} \end{equation}\] <p>We are now ready to test EFDA with a simulation.</p> <h4 id="worked-example-weibull-distribution">worked example: weibull distribution</h4> <p>We now test EFDA in practice. For this example, let us assume that our data in each class is distributed according to the <a href="https://en.wikipedia.org/wiki/Exponential_family">Weibull</a>, which does have an exponential family parametrization. We can express this distribution as \(\text{Weibull}(\lambda, k)\), where parameters \(\lambda &gt; 0\) (scale) and \(k &gt; 0\) (shape). Moreover, the Weibull PDF can be given by \(f(x) = \frac{k}{\lambda} (\frac{x}{\lambda})^{k - 1} \exp(-(\frac{x}{\lambda})^k)\) for \(x \geq 0\). We now state our assumptions for our data in each class:</p> \[X \mid Y = 0 \sim \text{Weibull}(\lambda_1, k) \quad X \mid Y = 1 \sim \text{Weibull}(\lambda_0, k)\] <p>Recall that our goal with EFDA is to be able to learn \(\lambda_1\) and \(\lambda_0\), as well as \(\alpha = \mathbb{P}[Y = 1]\), so as to be able to model classifier function \(p(X) = \mathbb{P}[Y = 1 \mid X]\). For this simulation, we specify the following ground truth values:</p> \[k = 3 \quad \lambda_1 = 2 \quad \lambda_0 = 4 \quad \alpha = 0.7\] <p>Note that \(\alpha = 0.7\) means that there is an overall bias towards data belonging in class one. We provide the following plot below to understand the distributions of our different data:</p> <div style="text-align: center;"> <img src="/assets/img/efda-post/weibull-data.png"/> </div> <p>While the above plot can feel slightly overwhelming, I would recommend pausing to make sure each curve makes sense. From it, we can see that roughly something like \(x = 2.5\) seems to be a good barrier between data in class one (left) and data in class zero (right). As a bit of foreshadowing, we’ll also compute the log-odds ratio for our data:</p> \[\log \frac{p(X)}{1 - p(X)} = \log \frac{\alpha f_1(x)}{(1 - \alpha) f_0(x)} = \log \frac{\alpha}{1 - \alpha} + \log \frac{\frac{k}{\lambda_1} (\frac{x}{\lambda_1})^{k - 1} \exp(-(\frac{x}{\lambda_1})^k)}{\frac{k}{\lambda_0} (\frac{x}{\lambda_0})^{k - 1} \exp(-(\frac{x}{\lambda_0})^k)} = \log \frac{\alpha}{1 - \alpha} + k\log \frac{\lambda_0}{\lambda_1} + x^k(\frac{1}{\lambda_0^k} - \frac{1}{\lambda_1^k})\] <p>and so the log-odds ratio are <em>not</em> a linear function of \(X\). We now proceed to actually use our derived EFDA procedure to model \(p(X)\) (green curve) from our given dataset \(\mathcal{D} = \{(X_i, Y_i)\}_{i = 1}^n\). As a first step, we give the exponential family parametrization of the Weibull PDF, for when \(k\) is known a priori:</p> \[\eta = -\frac{1}{\lambda^k}, \quad h(x) = x^{k - 1}, \quad T(x) = x^k, \quad A(\eta) = \log(-\frac{1}{\eta k}) = \log(\frac{1}{\eta}) + \log(-\frac{1}{k})\] <p>For a verification that this does indeed work see below:</p> <details> <summary> Verification of Weibull Exponential Family Parametrization </summary> $$ h(x) \exp[\eta \cdot T(x) - A(\eta)] = x^{k - 1} \exp[-\frac{x^k}{\lambda^k} + \log(\frac{k}{\lambda^k})] = x^{k - 1} \cdot \frac{k}{\lambda^k} \exp[-(\frac{x}{\lambda})^k] = \frac{k}{\lambda} (\frac{x}{\lambda})^{k - 1} \exp[-(\frac{x}{\lambda})^k] $$ which is exactly the PDF of a Weibull distribution. </details> <p><br/></p> <p>Recall that we are assuming that the true natural parameters \(\eta_1\) and \(\eta_0\) are different across each class. Now applying the derived MLE conditions stated in \eqref{efda-mle} for this specific exponential family parametrization, we can find the MLEs of \(\eta_1\) and \(\eta_0\). We start by deriving the former:</p> \[\sum_{i: Y_i = 1}^n T(X_i) = N_1 \cdot \frac{\partial A(\eta_1)}{\partial \eta_1} \implies \sum_{i: Y_i = 1}^n X_i^k = N_1 \cdot (-\frac{1}{\eta_1}) \implies \hat{\eta}_1 = -\frac{N_1}{\sum_{i: Y_i = 1}^n X_i^k }\] <p>and so by identical logic we have:</p> \[\hat{\eta}_0 = -\frac{N_0}{\sum_{i: Y_i = 0}^n X_i^k }\] <p>Thus, we now have all the necessary ingredients to perform EFDA on our given dataset. We first start by giving the code to generate samples from our data:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">CLASS_ONE_TRUE_LAMBDA</span> <span class="o">=</span> <span class="mi">2</span>
<span class="n">CLASS_ZERO_TRUE_LAMBDA</span> <span class="o">=</span> <span class="mi">4</span>
<span class="n">ALPHA_TRUE</span> <span class="o">=</span> <span class="mf">0.7</span>
<span class="n">K_TRUE</span> <span class="o">=</span> <span class="mi">5</span> <span class="c1"># common across both classes
</span>
<span class="k">def</span> <span class="nf">generate_data</span><span class="p">(</span><span class="n">n_samples</span><span class="p">):</span> 
    <span class="n">X_class_one</span> <span class="o">=</span> <span class="n">weibull_min</span><span class="p">.</span><span class="nf">rvs</span><span class="p">(</span><span class="n">c</span><span class="o">=</span><span class="n">K_TRUE</span><span class="p">,</span> <span class="n">scale</span><span class="o">=</span><span class="n">CLASS_ONE_TRUE_LAMBDA</span><span class="p">,</span> <span class="n">size</span><span class="o">=</span><span class="n">n_samples</span><span class="p">,</span> <span class="n">random_state</span><span class="o">=</span><span class="mi">42</span><span class="p">)</span>
    <span class="n">X_class_zero</span> <span class="o">=</span> <span class="n">weibull_min</span><span class="p">.</span><span class="nf">rvs</span><span class="p">(</span><span class="n">c</span><span class="o">=</span><span class="n">K_TRUE</span><span class="p">,</span> <span class="n">scale</span><span class="o">=</span><span class="n">CLASS_ZERO_TRUE_LAMBDA</span><span class="p">,</span> <span class="n">size</span><span class="o">=</span><span class="n">n_samples</span><span class="p">,</span> <span class="n">random_state</span><span class="o">=</span><span class="mi">42</span><span class="p">)</span>
    <span class="n">y</span> <span class="o">=</span> <span class="p">(</span><span class="n">np</span><span class="p">.</span><span class="n">random</span><span class="p">.</span><span class="nf">uniform</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="n">size</span><span class="o">=</span><span class="n">n_samples</span><span class="p">)</span> <span class="o">&lt;=</span> <span class="mf">0.7</span><span class="p">).</span><span class="nf">astype</span><span class="p">(</span><span class="nb">int</span><span class="p">)</span> 
    <span class="n">X</span> <span class="o">=</span> <span class="n">y</span> <span class="o">*</span> <span class="n">X_class_one</span> <span class="o">+</span> <span class="p">(</span><span class="mi">1</span> <span class="o">-</span> <span class="n">y</span><span class="p">)</span> <span class="o">*</span> <span class="n">X_class_zero</span>
    <span class="k">return</span> <span class="n">X</span><span class="p">,</span> <span class="n">y</span> 
</code></pre></div></div> <p>As a baseline, we’ll want to compare EFDA’s results to those of Logistic Regression. While we can’t do a direct comparison of their parameters (as they are inherently different), we’ll be interested in seeing how they model the log-odds differently. Specifically, across a number of different trials (each with different sampled datasets), we’ll want to see how Logistic’s Regression and EFDA’s log-odds ratio varies as a function of \(X\). We provide this plot below, across 100 different trials each with sample size \(n = 10^4\):</p> <div style="text-align: center;"> <img src="/assets/img/efda-post/weibull-log-reg-vs-efda.png"/> </div> <p>The major takeaway from this plot is that in cases where the true log-odds function is nonlinear (green curve), Logistic Regression will fail to model this nuance whereas a correctly-specified EFDA classifier will be able to. Furthermore, because Logistic Regression can only model the log-odds linearly, this means that for certain values of \(x\) (e.g. small \(x\)), it will overconfidently predict one class, and for other values of \(x\) (e.g. large \(x\)), it will underconfidently predict the other class.</p> <h4 id="further-considerations">further considerations</h4> <p>While EFDA did work quite well for this example with the Weibull distribution, note that it is not always guaranteed to do so. For instance, depending on the required \(A(\mathbf{\eta})\) function, the MLE conditions in \eqref{efda-mle} do not always lend themselves to a closed-form solution. As an example, the chi-squared exponential family parametrization necessitates \(A(\eta) = \log \Gamma(\eta + 1) + (\eta + 1)\log2\). Such a parametrization has an unwieldly derivative w.r.t \(\eta\) and likely will not have a closed-form solution for the MLE. Furthermore, EFDA, similar to LDA, suffers from the same strong assumption of a priori knowledge of the distribution of data in each class. Not to mention that this distribution must be the same among each class. As such, EFDA should be thought of as a bespoke solution with nice properties.</p> <h4 id="code">code</h4> <p>All code for the Weibull example can be found in this <a href="https://github.com/anish-lakkapragada/exponential-family-discriminant-analysis">file</a>.</p> <h4 id="resources">resources</h4> <p>I list all my sources for this article below.</p> <ol> <li>For a great introduction to LDA &amp; QDA, please see these <a href="http://www.stat.yale.edu/~zf59/sds242/Lecture25.pdf">slides</a> from Yale’s S&amp;DS 242.</li> <li>For understanding the exponential family for single and multivariate parametric sets, <a href="https://en.wikipedia.org/wiki/Exponential_family">Wikipedia</a> was of much help.</li> </ol> <p>If you find this piece helpful, please cite this blog post below:</p> <div class="language-bibtex highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">@article</span><span class="p">{</span><span class="nl">exponential-family-discriminant-analysis</span><span class="p">,</span>
  <span class="na">title</span>   <span class="p">=</span> <span class="s">{Exponential Family Discriminant Analysis}</span><span class="p">,</span>
  <span class="na">author</span>  <span class="p">=</span> <span class="s">{Lakkapragada, Anish}</span><span class="p">,</span>
  <span class="na">year</span>    <span class="p">=</span> <span class="s">{2025}</span><span class="p">,</span>
  <span class="na">month</span>   <span class="p">=</span> <span class="s">{May}</span><span class="p">,</span>
  <span class="na">url</span>     <span class="p">=</span> <span class="s">{https://anish.lakkapragada.com/blog/2025/efda/}</span>
<span class="p">}</span>
</code></pre></div></div> <p><em>Thank you for reading this blog post. If you have any questions or noticed any errata, please <a href="mailto:anish.lakkapragada@yale.edu">email me</a>.</em></p> <div class="footnotes" role="doc-endnotes"> <ol> <li id="fn:bayes-classifier-note"> <p>Formally speaking, a Bayes Classifier is a classifier with the lowest possible missclassification rate (zero-one loss) compared to any other classifier on the same set of features. For a formal proof that \(h(x)\) is a Bayes classifier, <a href="https://en.wikipedia.org/wiki/Bayes_classifier">see here</a>. <a href="#fnref:bayes-classifier-note" class="reversefootnote" role="doc-backlink">&#8617;</a></p> </li> <li id="fn:qda"> <p>Note that if we were to suppose that each class has its class-dependent covariance matrix \(\Sigma_k\), the log-odds ratio would be modeled as a quadratic function of \(x\). This technique is known as <em>Quadratic</em> Discriminant Analysis. <a href="#fnref:qda" class="reversefootnote" role="doc-backlink">&#8617;</a></p> </li> <li id="fn:why"> <p>The reason for why this is technically misleading is that the terminology for LDA/QDA is based on how the log-odds function is related to \(X\). As we will see, this can differ based on the type of distribution our data takes (regardless of it is in the exponential family.) <a href="#fnref:why" class="reversefootnote" role="doc-backlink">&#8617;</a></p> </li> </ol> </div>]]></content><author><name></name></author><category term="statistics"/><category term="lda, exponential-families, ml"/><summary type="html"><![CDATA[generalizing linear discriminant analysis beyond normally distributed data]]></summary></entry><entry><title type="html">bernstein-von mises theorem &amp;amp; power posteriors</title><link href="https://anish-lakkapragada.github.io/blog/2025/bayesian-misspecification/" rel="alternate" type="text/html" title="bernstein-von mises theorem &amp;amp; power posteriors"/><published>2025-05-13T00:00:00+00:00</published><updated>2025-05-13T00:00:00+00:00</updated><id>https://anish-lakkapragada.github.io/blog/2025/bayesian-misspecification</id><content type="html" xml:base="https://anish-lakkapragada.github.io/blog/2025/bayesian-misspecification/"><![CDATA[<meta name="viewport" content="width=device-width, initial-scale=1.0"/> <p>Hey all! Hope you are doing well. In this post, I aim to cover some results regarding Bayesian inference in model misspecification settings and then actually test them with a short simulation. Let us establish some preliminaries: consider any parameter estimation procedure (e.g. MoM, MLE, MAP) which posits \(X_1, \dots, X_n \overset{\text{i.i.d}}{\sim} f(x \mid \theta)\) whereas in truth \(X_1, \dots, X_n \overset{\text{i.i.d}}{\sim} g\) for some unknown \(g\).</p> <p>Recall that MLEs \(\hat{\theta}\) have a well-studied asymptotic distribution in this situation, the sandwich variance<sup id="fnref:term-note"><a href="#fn:term-note" class="footnote" rel="footnote" role="doc-noteref">1</a></sup>. Written for the univariate case, this can be given by<sup id="fnref:reg-conditions"><a href="#fn:reg-conditions" class="footnote" rel="footnote" role="doc-noteref">2</a></sup>:</p> \[\sqrt{n}[\hat{\theta} - \theta^*] \overset{d}{\to} \mathcal{N}(0, \frac{\mathbb{E}_g[(\frac{\partial}{\partial \theta} \log f(X \mid \theta^*))^2]}{\mathbb{E}_g[\frac{\partial^2}{\partial \theta^2} \log f(X \mid \theta^*)]^2}) \quad \theta^* = \underset{\theta}{\mathrm{argmin}} \ \text{KL}(g(x) \mid\mid f(x \mid \theta))\] <p>as per this <a href="/notes/s&amp;ds-242/Sandwich_Variance.pdf">short derivation</a>. For notation purposes, let us define this sandwich variance as \(V_{\text{sand}}\). But what about for bayesian inference? As per standard bayesian inference, suppose we assume \(\theta\) is modeled by random variable \(\Theta\) which follows some specified prior. The <em>Bernstein-von Mises theorem</em> tells us that in cases of correct model misspecification, informally for large \(n\), we can write \(\Theta \mid \textbf{X} \overset{\mathrm{approx}}{\sim} \mathcal{N}(\hat{\theta}, \frac{1}{nI(\hat{\theta})})\) where \(I(\theta)\) is the Fisher Information. In cases of model misspecification, Bernwise-von Mises theorem provides an adjustment of the kind we would expect:</p> <p>\begin{equation} \sqrt{n}[\Theta - \theta^* \mid \mathbf{X}] \overset{d}{\to} \mathcal{N}(0, V_{\text{sand}}) \label{bvm-misspecified} \end{equation}</p> <p>Nice! We have some understanding of how our posteriors will look in the case of model misspecification, albeit only <em>asymptotically</em>. But what if we could control the rate at which our (misspecified) posterior adjusts to new data? In other words, what if we could make the convergence of \(\Theta \mid \mathbf{X}\) to \eqref{bvm-misspecified} slower.</p> <h4 id="power-posteriors">power posteriors</h4> <p>This is where <em>power posteriors</em>, a rather new method, come into play. For a power posterior \(\Theta \mid \mathbf{X}\) where $\Theta$ is distributed according to prior \(f_{\Theta}(\theta)\), the posterior PDF is given by:</p> \[f_{\Theta \mid \mathbf{X}}(\theta \mid \mathbf{X}) = \frac{f_{\mathbf{X} \mid \Theta}(\mathbf{X} \mid \theta)^{\gamma} f_{\Theta}(\theta)}{f(X)} = \frac{f_{\Theta}(\theta) \prod_{i = 1}^n f(x_i \mid \theta)^{\gamma}}{\int_{\Theta} f_{\mathbf{X} \mid \Theta}(\mathbf{X} \mid \theta')^{\gamma} f_{\Theta}(\theta') d\theta' } , \quad \quad \gamma \in [0, 1]\] <p>The intuitive idea is to decrease the weightage of the likelihood terms by raising them to some power \(\gamma \leq 1\) (note for \(\gamma = 1\) this is just our normal posterior).</p> <p>Note that there have been <a href="https://jwmi.github.io/talks/BNP11.pdf">three discussed interpretations</a> of the power posterior. While we won’t derive them from theory, one that I like and makes intuitive sense is that the power posterior reduces the sample size from \(n\) to \(n\gamma\) (essentially we will need more samples to get the same concentrated (power) posterior<sup id="fnref:sample-size-note"><a href="#fn:sample-size-note" class="footnote" rel="footnote" role="doc-noteref">3</a></sup>.)</p> <p>Finally, it has been found that power posteriors do have nice asymptotics. In this <a href="https://arxiv.org/pdf/2310.07900">paper</a>, they were able to find that power posteriors do converge to the limiting distributions of Bernstein-von Mises Theorem.</p> <h4 id="worked-model-misspecification-example">worked model misspecification example</h4> <p>We conclude this blog post with some visualizations of these concepts in action, starting with Bernstein-von Mises Theorem. Let us assume we have samples \(X_1, \dots, X_n\) which are truly distributed by $\text{Gamma}(3, 2)$. Note that we are using the shape-rate parametrization of the Gamma distribution for this blog post. Now for the model misspecification: we assume our data is distributed by \(\text{Expo}(\lambda)\) and set a prior $\lambda \sim \text{Gamma}(1, 1)$. Through standard Bayesian Inference, we arrive at the following clean posterior:</p> \[\lambda \mid \mathbf{X} \sim \text{Gamma}(1 + n, 1 + \sum_{i = 1}^n X_i)\] <p>We aim to show that this posterior follows our established result \eqref{bvm-misspecified}. To do so, we must compute the <em>pseudo-true value</em> \(\lambda^*\). We provide a quick derivation below, where $g(x)$ gives the true PDF for $\text{Gamma}(3, 2)$:</p> \[\text{KL}(g(x) \mid\mid f(x \mid \lambda)) = \mathbb{E}_g[\log \frac{g(x)}{f(x \mid \lambda)}] = \mathbb{E}_g[\log g(x)] - \mathbb{E}_g[\log f(x \mid \lambda)]\] \[\implies \lambda^* = \underset{\lambda}{\mathrm{argmin}} \ \ \mathbb{E}_g[\log f(x \mid \lambda)] = \underset{\lambda}{\mathrm{argmax}} \ \ \mathbb{E}_g[\log (\lambda) - \lambda x] = \underset{\lambda}{\mathrm{argmax}} \ \ \log(\lambda) - \lambda\mathbb{E}_g[x]\] \[\implies \lambda^{*} = \frac{1}{\mathbb{E}_{g}[x]} = \frac{1}{\frac{3}{2}} = \frac{2}{3}\] <p>We can now show the below visualization demonstrating Bernstein-von Mises Theorem as we increase \(n\):</p> <div style="text-align: center;"> <img src="/assets/img/bayesian-misspecification/bvm_demo.gif" style="max-width: 90%; height: auto;"/> </div> <p>Thus, we can see that our posterior \(\lambda \mid \mathbf{X}\) does indeed converge in distribution to a normal around \(\lambda^*\).</p> <p>We move onto trying to visualize power posteriors, specifically the effect of \(\gamma\). To do so, we’ll need to derive the derivation of our posterior, for any value of \(\gamma\). We start with our posterior distribution PDF:</p> \[f_{\mathbf{\lambda} \mid \mathbf{X}}(\lambda \mid \mathbf{X}) \propto f_{\mathbf{\lambda}}(\lambda) \prod_{i = 1}^n f(x_i \mid \lambda)^{\gamma} \implies f_{\mathbf{\lambda} \mid \mathbf{X}}(\lambda \mid \mathbf{X}) \propto e^{-\lambda} \prod_{i = 1}^n \lambda^{\gamma} e^{-\lambda \gamma x_i}\] \[\implies f_{\mathbf{\lambda} \mid \mathbf{X}}(\lambda \mid \mathbf{X}) \propto \lambda^{n\gamma} \text{exp}(-\lambda(1 + \gamma \sum_{i = 1}^n x_i))\] <p>and so we can conclude $\lambda \mid \mathbf{X} \sim \text{Gamma}(n\gamma + 1, 1 + \gamma \sum_{i = 1}^n x_i)$. Thus for any specified value of \(0\leq \alpha \leq 1\), we have a neat form of the power posterior distribution. Note that if you compare this to the form of our previously derived posterior, the “reducing the sample size” interpretation of power posteriors becomes blatantly clear. We now provide a similar animation as before to show how quickly the (power) posterior(s) converge normally around \(\lambda^*\) as \(n\) increases.</p> <div style="text-align: center;"> <img src="/assets/img/bayesian-misspecification/power_posteriors.gif" style="max-width: 90%; height: auto;"/> </div> <p>This behavior matches what we should expect (as per the preivously discussed asymptotic result of power posteriors): lower values of \(\gamma\) will engender posteriors which adapt to new observations slower.</p> <h4 id="code">code</h4> <p>The code for generating the first visualization showing Bernstein-von Mises Theorem can be found <a href="https://github.com/anish-lakkapragada/bvm-power-posteriors/blob/main/bvm_visualization.py">here</a>. Code for visualization of power posteriors can be found <a href="https://github.com/anish-lakkapragada/bvm-power-posteriors/blob/main/power_posterior_visualization.py">here</a>.</p> <h4 id="sources">sources</h4> <p>Power posteriors are not a frequently discussed topic, so I am super grateful to the below resources:</p> <ol> <li>This <a href="https://jwmi.github.io/talks/BNP11.pdf">talk</a> from Jeff Miller on understanding the power posterior</li> <li>This <a href="https://arxiv.org/pdf/2310.07900">paper</a> whch gives the two really nice results on power posterior asymptotics</li> <li>This <a href="https://andrewcharlesjones.github.io/journal/power-posteriors.html">blog article</a> on power posteriors which also provided some nice visualizations</li> </ol> <p><em>Thank you for reading this blog post. If you have any questions or noticed any errata, please <a href="mailto:anish.lakkapragada@yale.edu">email me</a>.</em></p> <div class="footnotes" role="doc-endnotes"> <ol> <li id="fn:term-note"> <p>Unfortunately, the reasoning for this name is only very clear in the <a href="https://www.stat.umn.edu/geyer/5601/notes/sand.pdf">multivariable case</a>. <a href="#fnref:term-note" class="reversefootnote" role="doc-backlink">&#8617;</a></p> </li> <li id="fn:reg-conditions"> <p>For this statement there are some regularity conditions which I know must be satisifed but that I am unknowledgeable about. Apologies. <a href="#fnref:reg-conditions" class="reversefootnote" role="doc-backlink">&#8617;</a></p> </li> <li id="fn:sample-size-note"> <p>For a slightly more technical justification, look no further than the log density of the power posterior: \(\log f_{\Theta \mid \mathbf{X}}(\Theta \mid \mathbf{X}) \propto f_{\Theta}(\theta) + \gamma \sum_{i = 1}^n \log f(x_i \mid \theta)\). <a href="#fnref:sample-size-note" class="reversefootnote" role="doc-backlink">&#8617;</a></p> </li> </ol> </div>]]></content><author><name></name></author><category term="statistics"/><category term="bayesian-inference"/><summary type="html"><![CDATA[bayesian inference in model misspecification settings, visually explained]]></summary></entry><entry><title type="html">what if we didn’t approximate posteriors?</title><link href="https://anish-lakkapragada.github.io/blog/2025/bnn/" rel="alternate" type="text/html" title="what if we didn’t approximate posteriors?"/><published>2025-05-09T00:00:00+00:00</published><updated>2025-05-09T00:00:00+00:00</updated><id>https://anish-lakkapragada.github.io/blog/2025/bnn</id><content type="html" xml:base="https://anish-lakkapragada.github.io/blog/2025/bnn/"><![CDATA[<meta name="viewport" content="width=device-width, initial-scale=1.0"/> <p><em>TLDR: Comparing predictive distributions from posteriors estimated by coordinate-ascent variational inference (CAVI) versus exact derivation in a univariate bayesian linear regression setting.</em></p> <p>Hey all! Just finished my freshman year of college and thought it would be fun to explore one of the things we learned in my <a href="http://www.stat.yale.edu/~zf59/sds242">statistical inference</a> class in more depth. Specifically, I am interested in exploring bayesian inference by exploring predictive distributions.</p> <h4 id="bayesian-neural-networks-why-what-how">bayesian neural networks: why, what, how</h4> <p>At a high level, bayesian neural networks help us understand the distribution of parameters our model could have taken, conditional on some observed data. This contrasts with standard neural networks, which only focus on learning the optimal parameters (at the risk of overfitting). In other words, given some data $D$ and a model with parameter weights $\theta$, BNNs aim to compute the posterior:</p> \[p(\theta \mid D) = \frac{p(D \mid \theta) p(\theta)}{\displaystyle \int_{\theta'} p(D \mid \theta') p(\theta') d\theta'}\] <p>for some chosen prior distribution $p(\theta)$. Setting aside how BNNs actually find this posterior (the subject of this blog post), this posterior then lets us compute predictive distributions for new inputs. For a test point $x$, we have:</p> \[p\bigl(\hat y \mid D, x\bigr) = \int p\bigl(\hat y \mid \theta, x)\,p\bigl(\theta \mid D)\,\mathrm{d}\theta\] <p>We now get into the computability issue for the posterior. As is likely no surprise, the evidence term $p(D)$ in the denominator is usually intractable once $\theta$ is high-dimensional. Two classes of methods are typically used to approximate the posterior: (1) Markov Chain Monte Carlo (MCMC) algorithms like <a href="https://blog.djnavarro.net/posts/2023-04-12_metropolis-hastings/">Metropolis Hastings</a> (which exploits the fact that $p(\theta \mid D) \propto p(D \mid \theta)\,p(\theta)$ from Bayes’ Rule) and (2) variational inference which approximates $p(\theta \mid D)$ by learning a surrogate function $q_{\phi}(\theta)$ parametrized by weights $\phi$. For our purposes, we focus on (2) in this article as (1) is often computationally expensive when $\theta$ is high dimensional. While an entire <a href="https://arxiv.org/pdf/1601.00670">paper</a> can be written on variational inference, we give a brief explanation here.</p> <p>For our purposes let us assume each weight in $\theta$ is independent from each other and has zero covariance. Variational Inference works by learning weights $\phi$ to minimize the KL divergence between distribution $q_{\phi}(\theta)$ and our posterior $p(\theta \mid D)$. Note however that we cannot directly minimize this KL divergence due to its intractable $p(D)$ term, as shown below<sup id="fnref:kl-note"><a href="#fn:kl-note" class="footnote" rel="footnote" role="doc-noteref">1</a></sup>:</p> \[% \begin{aligned} % \mathrm{KL}(q_{\phi}(\theta)\,\|\,p(\theta \mid D)) &amp;= \mathbb{E}[\log q_{\phi}(\theta)] \\ % - \mathbb{E}[\log p(\theta, D)] + \log p(D) % \end{aligned} % \small \begin{aligned} \mathrm{KL}(q_{\phi}(\theta)\,\|\,p(\theta \mid D)) = \mathbb{E}[\log q_{\phi}(\theta)] - \mathbb{E}[\log p(\theta \mid D)] \\ = \mathbb{E}[\log q_{\phi}(\theta)] - \mathbb{E}[\log p(\theta, D)] + \log p(D) \end{aligned}\] <p>Because $\log p(D)$ is just a constant, it is sufficient to minimize this KL by maximizing the <em>evidence lower bound</em> (ELBO):</p> \[\mathbb{E}[\log p(\theta, D)] \;-\;\mathbb{E}[\log q_{\phi}(\theta)].\] <p>While plenty of methods exist for doing this, we can focus on one by making the strong assumption that each parameter in $\theta$ has its own independent distribution. Such a assumption brings us to the land of <em>mean-field variational inference</em>, where we factorize our approximate posterior $q_{\phi}(\theta)$ into:</p> \[q_{\phi}(\theta) = \prod_{i} q_{\phi_i}(\theta_i),\] <p>where each $q_{\phi_i}$ is our estimated PDF of parameter $\theta_i$ parametrized by weights $\phi_i$. Using this form lends itself nicely to <em>Coordinate Ascent Variational Inference</em> (CAVI), where we iteratively maximize the ELBO with respect to one factor ($\phi_i$) while holding the others fixed. CAVI is quite a non-trivial algorithm, and we’ll explore it more when we use it later<sup id="fnref:cavi-note"><a href="#fn:cavi-note" class="footnote" rel="footnote" role="doc-noteref">2</a></sup>.</p> <p>Now, given this fully‐trained approximate stand‐in distribution $q_{\phi}(\theta)$ for the true posterior $p(\theta \mid D)$, we can compute predictive distributions as defined before. Instead of tackling that integral directly, we typically use Monte Carlo integration: sampling i.i.d. parameters $q_{\phi}(\theta)$ and averaging their predictions.</p> <h4 id="what-were-curious-about">what we’re curious about</h4> <p>Regardless of the choice of MCMC or variational inference, the bottom line is that the posterior distribution is always approximated. But what if we could exactly pinpoint the true posterior? I am interested in comparing predictive distributions from an approximate CAVI-trained posterior versus an exactly computed posterior. Specifically, we will inspect this in the case of univariate (bayesian) linear regression.</p> <h4 id="step-one-defining-the-problem-statement">step one: defining the problem statement</h4> <p>Let us first define our data $D$ in matrix form as:</p> \[y = X\beta + \epsilon \quad \beta = \begin{pmatrix} \beta_0 \\ \beta_1 \end{pmatrix} \quad X = \begin{bmatrix} 1 &amp; x_1 \\ \vdots &amp; \vdots \\ 1 &amp; x_n \end{bmatrix}\] <p>where $\epsilon \sim \mathcal{N}(0, \sigma^2) $ for some known $\sigma^2$. Note that we are treating observations $x_1, \dots, x_n$ as fixed. We place the following multivariate normal prior on $\beta$:</p> \[\beta = \begin{pmatrix} \beta_0 \\ \beta_1 \end{pmatrix} \sim \mathcal{N}(\mathbf{0}, S) \quad S = \begin{pmatrix} \tau_0^2 &amp; 0 \\ 0 &amp; \tau_1^2 \end{pmatrix}\] <p>for some chosen $\tau_0, \tau_1$. For convenience, we’ll choose $\tau_0 = \tau_1 = 1$ and so we’ll model $\beta$ with the multivariate standard normal distribution $\mathcal{N}(\mathbf{0}, \mathbf{I})$. Furthermore, we’ll set the true residual variance $\sigma^2$ to one and assume it is known. We are now ready to start coding! Let us define this function to generate $D$ from:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">numpy</span> <span class="k">as</span> <span class="n">np</span>
<span class="kn">from</span> <span class="n">scipy</span> <span class="kn">import</span> <span class="n">stats</span>

<span class="n">SIGMA_SQUARED</span> <span class="o">=</span> <span class="mi">1</span>

<span class="c1"># generate D
</span><span class="k">def</span> <span class="nf">generate_data</span><span class="p">(</span><span class="n">n</span><span class="o">=</span><span class="mi">100</span><span class="p">):</span>
    <span class="n">beta_0_true</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="n">random</span><span class="p">.</span><span class="nf">random</span><span class="p">()</span> <span class="o">*</span> <span class="mi">3</span>
    <span class="n">beta_1_true</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="n">random</span><span class="p">.</span><span class="nf">random</span><span class="p">()</span> <span class="o">*</span> <span class="mi">3</span>
    <span class="n">X</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="n">random</span><span class="p">.</span><span class="nf">normal</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="n">n</span><span class="p">)</span>
    <span class="n">eps</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="n">random</span><span class="p">.</span><span class="nf">normal</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="n">SIGMA_SQUARED</span><span class="p">,</span> <span class="n">n</span><span class="p">)</span>
    <span class="n">y</span> <span class="o">=</span> <span class="n">beta_0_true</span> <span class="o">+</span> <span class="n">beta_1_true</span> <span class="o">*</span> <span class="n">X</span> <span class="o">+</span> <span class="n">eps</span>
    <span class="k">return</span> <span class="n">X</span><span class="p">,</span> <span class="n">y</span>
</code></pre></div></div> <p>Note that this generation process does generate a linear relationship with a (likely) non-trivial difference from its prior.</p> <p>We now cover the two ways to obtain our posterior $p(\beta \mid D)$: deriving the exact posterior or approximating it with variational inference. We start with the former.</p> <h4 id="step-two-deriving-the-exact-posterior">step two: deriving the exact posterior</h4> <p>I cheated a little bit. Our current univariate linear regression problem is a well-studied problem of <em>bayesian linear regression</em> (BLR) where the exact posterior of parameters $\beta$ is well-known. Deriving this exact posterior does require some matrix algebra, but just for completeness we provide a quick derivation (following these <a href="https://www.cs.toronto.edu/~rgrosse/courses/csc411_f18/slides/lec19-slides.pdf">class slides</a>). For a more thorough derivation that goes through the complete-the-square trick below, see this excellent <a href="https://medium.com/intuition/gentle-introduction-of-bayesian-linear-regression-c83da6b0d1f7">Medium article</a>.</p> \[\begin{aligned} \log p(\beta\mid D) &amp;= \log p(\beta) + \log p(D\mid \beta) + \text{const} \\ &amp;= -\tfrac12\,\beta^T S^{-1}\beta - \tfrac{1}{2\sigma^2}\,\bigl\lVert X\beta - y\bigr\rVert^2 + \text{const} \\ &amp;= -\tfrac12\,\beta^T S^{-1}\beta - \tfrac{1}{2\sigma^2}\bigl(\beta^T X^T X \beta - 2\,y^T X \beta + y^T y\bigr) + \text{const} \\ &amp;= -\tfrac12(\beta - \mu)^T \Sigma^{-1}(\beta - \mu) + \text{const} \end{aligned} % \begin{array}{l} % \log p(\beta \mid D) % = \log p(\beta) + \log p(D \mid \beta) + \text{const} \\ % = -\tfrac{1}{2}\,\beta^T S^{-1}\beta % \,-\, \tfrac{1}{2\sigma^2}\,\bigl\lVert X\beta - y\bigr\rVert^2 % \,+\, \text{const} \\ % = -\tfrac{1}{2}\,\beta^T S^{-1}\beta % \,-\, \tfrac{1}{2\sigma^2}\bigl(\beta^T X^T X \beta % - 2\,y^T X \beta + y^T y\bigr) % \,+\, \text{const} \\ % = -\tfrac{1}{2}(\beta - \mu)^T \Sigma^{-1}(\beta - \mu) + \text{const} % \end{array}\] <p>where we define $\mu = \tfrac{1}{\sigma^2} \Sigma(X^Ty)$ and $\Sigma^{-1} = \tfrac{1}{\sigma^2} X^TX + S^{-1}$. Note that our last line shows that $p(\beta \mid D)$ takes the form of a multivariate normal distribution, namely we can express:</p> \[\beta \mid D \sim \mathcal{N}(\mu, \Sigma)\] <p>This concludes the derivation for the <em>exact</em> posterior distribution. It’ll be helpful to implement a function to sample from this posterior (we’ll see why in step three):</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">S</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">array</span><span class="p">([[</span><span class="mi">1</span><span class="p">,</span> <span class="mi">0</span><span class="p">],</span> <span class="p">[</span><span class="mi">0</span><span class="p">,</span> <span class="mi">1</span><span class="p">]])</span>
<span class="k">def</span> <span class="nf">sample_from_blr_posterior</span><span class="p">(</span><span class="n">X</span><span class="p">,</span> <span class="n">y</span><span class="p">,</span> <span class="n">num_samples</span><span class="o">=</span><span class="mi">1000</span><span class="p">):</span>
    <span class="n">X_expanded</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">ones</span><span class="p">((</span><span class="n">X</span><span class="p">.</span><span class="n">shape</span><span class="p">[</span><span class="mi">0</span><span class="p">],</span> <span class="mi">2</span><span class="p">))</span>
    <span class="n">X_expanded</span><span class="p">[:,</span> <span class="mi">1</span><span class="p">]</span> <span class="o">=</span> <span class="n">X</span>

    <span class="n">Sigma</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="n">linalg</span><span class="p">.</span><span class="nf">inv</span><span class="p">((</span><span class="mi">1</span> <span class="o">/</span> <span class="n">SIGMA_SQUARED</span><span class="p">)</span> <span class="o">*</span> <span class="n">np</span><span class="p">.</span><span class="nf">matmul</span><span class="p">(</span><span class="n">X_expanded</span><span class="p">.</span><span class="n">T</span><span class="p">,</span> <span class="n">X_expanded</span><span class="p">)</span> <span class="o">+</span> <span class="n">np</span><span class="p">.</span><span class="n">linalg</span><span class="p">.</span><span class="nf">inv</span><span class="p">(</span><span class="n">S</span><span class="p">))</span>
    <span class="n">mu</span> <span class="o">=</span> <span class="p">(</span><span class="mi">1</span> <span class="o">/</span> <span class="n">SIGMA_SQUARED</span><span class="p">)</span> <span class="o">*</span> <span class="n">np</span><span class="p">.</span><span class="nf">matmul</span><span class="p">(</span><span class="n">Sigma</span><span class="p">,</span> <span class="n">np</span><span class="p">.</span><span class="nf">matmul</span><span class="p">(</span><span class="n">X_expanded</span><span class="p">.</span><span class="n">T</span><span class="p">,</span> <span class="n">y</span><span class="p">))</span>

    <span class="k">return</span> <span class="n">np</span><span class="p">.</span><span class="n">random</span><span class="p">.</span><span class="nf">multivariate_normal</span><span class="p">(</span><span class="n">mean</span><span class="o">=</span><span class="n">mu</span><span class="p">,</span> <span class="n">cov</span><span class="o">=</span><span class="n">Sigma</span><span class="p">,</span> <span class="n">size</span><span class="o">=</span><span class="n">num_samples</span><span class="p">)</span>
</code></pre></div></div> <p>Note that we <em>are</em> assuming knowledge of $\sigma^2$, the residual variance (as opposed to estimating it.) We are ready to try the second method to obtain our posterior!</p> <h4 id="step-two-and-a-half-approximating-the-posterior-with-variational-inference">step two (and a half): approximating the posterior with variational inference</h4> <p>Let us first establish the form of our stand-in distribution $q_{\phi}(\theta)$ with the (correct) assumption that $\beta_0$ and $\beta_1$ are independent $\implies$ we can use mean-field variational inference and thus represent $q_{\phi}(\beta) = q_{\phi_0}(\beta_0) \times q_{\phi_1}(\beta_1)$. We choose to define $q_{\phi_0}(\beta_0)$ and $q_{\phi_1}(\beta_1)$ by:</p> \[\forall j \in [0, 1], q_{\phi_j}(\beta_j) = f(\beta_j \mid m_j, s_j^2)\] <p>where $m_j, s_j^2$ are our learnable parameters. Expressed differently, this form of the approximated posterior posits each $\beta_j$ has its independent normal distribution or that each $\beta_j \sim \mathcal{N}(m_j, s_j^2)$. As previously mentioned, the procedure to now optimize $q_{\phi}(\theta)$ is <em>Coordinate-Ascent Variational Inference</em> (CAVI). While deriving CAVI is above my paygrade, at a high level CAVI aims to iteratively optimize the ELBO w.r.t to one mean-field posterior $q_{\phi_j}(\beta_j)$ while keeping all other posteriors $q_{\phi_{-j}}(\beta_{-j})$ fixed. Specifically, in a given CAVI iteration, we iterate through all parameters and update the $j$th parameter set $\phi_j$ s.t. the following condition is met:</p> \[q_{\phi_j}(\beta_j) \propto \exp(\mathbb{E}_{-j}[\log p(\beta_j \mid \beta_{-j}, y)]) \iff \log[q_{\phi_j}(\beta_j)] = \mathbb{E}_{-j}[\log p(\beta, y)] + \text{const.}\] <p>where \(\mathbb{E}_{-j}\) means that we assume \(\forall i \neq j, \beta_i \sim q_{\phi_i}(\beta_i)\). Comparing this condition and our previous ELBO derivation, we can see that it does sort of set $q_{\phi_j}(\beta_j)$ in a way that optimizes an ELBO adjusted for single coordinate optimization. For a formal treatment of how this maximizes the ELBO, see these <a href="https://www.cs.princeton.edu/courses/archive/fall11/cos597C/lectures/variational-inference-i.pdf">lecture notes</a>. We now apply this CAVI algorithm to our defined problem by finding how we should update our parameter sets $\phi_0 = (m_0, s_0^2)$ and $\phi_1 = (m_1, s_1^2)$ at each iteration to satisfy our aforementioned condition. Before doing so, it will be helpful to compute \(\log p(\beta, y)\), or equivalently \(\log p(\beta, y \mid x)\) as we are treating \(\mathbf{x}\) as fixed:</p> \[\begin{aligned} \log p(\beta, y \mid X) &amp;= \log p(\beta) + \log p(y \mid \beta, X) \\ &amp;= -\tfrac12 \beta^T S^{-1} \beta - \tfrac{1}{2\sigma^2} \sum_{i = 1}^n (y_i - \beta_0 - \beta_1 x_i)^2 + \text{const} \\ \end{aligned}\] <p>Because $S^{-1} = \text{diag}(\tau_0^{-2}, \tau_1^{-2})$, we can compactly represent this as:</p> \[\log p(\beta, y | X) = -\frac{1}{2\tau_0^2} \beta_0^2 - \frac{1}{2\tau_1^2} \beta_1^2 - \frac{1}{2\sigma^2} \sum_{i = 1}^n (y_i - \beta_0 - \beta_1 x_i)^2 + \text{const}\] <p>For short hand, we refer to the above expression as \(\log p(\beta, y)\). We are now ready to derive the update rule for $\phi_0$. For $\phi_0$, we must satisfy the following condition with our new approximated posterior \(q_{\phi_0}^*(\beta_0)\):</p> \[\log[q_{\phi_0}^*(\beta_0)] = \mathbb{E}_{q_{1}}[\log p(\beta, y)] + \text{const}\] <p>We express a meaningful <em>additive proportionality</em> relationship of \(\mathbb{E}_{q_1} [\log p(\beta, y)]\) below, disregarding constants w.r.t to \(\beta_0\). For our purposes, let \(\propto'\) define additive proportionality (i.e. $x \propto’ y \implies x = y + \text{const}$):</p> \[\begin{aligned} \mathbb{E}_{q_{1}}[\log p(\beta, y)] &amp;\propto' \mathbb{E}_{q_{1}}[-\frac{1}{2\tau_0^2} \beta_0^2 - \frac{1}{2\tau_1^2} \beta_1^2 - \frac{1}{2\sigma^2} \sum_{i = 1}^n (y_i - \beta_0 - \beta_1 x_i)^2] \\ &amp;\propto' -\frac{1}{2\tau_0^2} \beta_0^2 - \frac{1}{2\sigma^2} \sum_{i = 1}^n \mathbb{E}_{q_{1}}[(y_i - \beta_0 - \beta_1 x_i)^2] \\ &amp;\propto' -\frac{1}{2\tau_0^2} \beta_0^2 - \frac{1}{2\sigma^2} \sum_{i = 1}^n (y_i - x_i m_1 - \beta_0)^2 \\ &amp;\propto' -\frac{1}{2\tau_0^2} \beta_0^2 - \frac{1}{2\sigma^2} \sum_{i = 1}^n (\beta_0^2 - 2\beta_0(y_i - x_i m_1)) \\ &amp;\propto' -(\frac{1}{2\tau_0^2} + \frac{n}{2\sigma^2}) \beta_0^2 + \frac{1}{\sigma^2} \sum_{i = 1}^n (y_i - x_im_1) \beta_0 \end{aligned}\] <p>Note that by our update condition, we must have \(\log[q_{\phi_0}^*(\beta_0)]\) additively proportional to our above derived expression. Our above expression matches the log-density form of \(\mathcal{N}(m_{0, *}, s_{0, *}^{2})\) where we define:</p> \[s_{0, *}^2 = (\tau_0^{-2} + n\sigma^{-2})^{-1} \quad m_{0, *} = \frac{s_{0, *}^2}{\sigma^2} \sum_{i = 1}^n (y_i - x_im_1)\] <p>and so this serves as our update rule for $q_{\phi_0}(\beta_0)$. We now derive the update rule for \(q_{\phi_1}(\beta_1)\), albeit slightly more tersely this time:</p> \[\begin{aligned} \log[q_{\phi_1}^*(\beta_1)] \propto \mathbb{E}_{q_{0}}[\log p(\beta, y)] &amp;\propto' -\frac{1}{2\tau_1^2} - \frac{1}{2\sigma^2} \sum_{i = 1}^n \mathbb{E}_{q_{0}}(y_i - \beta_0 - \beta_1x_i)^2 \\ &amp;\propto' -\frac{1}{2\tau_1^2} -\frac{1}{2\sigma^2} \sum_{i = 1}^n (y_i - m_0 - \beta_1x_i)^2 \\ &amp;\propto' -\frac{1}{2}(\tau_1^{-2} + \sigma^{-2} \sum_{i = 1}^n x_i^2) \beta_1^2 + [\frac{1}{\sigma^2} \sum_{i = 1}^n x_i(y_i - m_0)] \beta_1 \end{aligned}\] <p>This matches the log density of $\mathcal{N}(m_{1, *}, s_{1, *}^2)$ where we define:</p> \[s_{1, *}^2 = (\tau_1^{-2} + \sigma^{-2} \sum_{i = 1}^n x_i^2)^{-1} \quad m_{1, *} = \frac{s_{1, *}^2}{\sigma^2} \sum_{i = 1}^n x_i(y_i - m_0)\] <p>So we have derived the update rules to perform CAVI. All that is left is to actually code these updates, which shouldn’t be too bad:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">TAU_0_SQUARED</span> <span class="o">=</span> <span class="n">TAU_1_SQUARED</span> <span class="o">=</span> <span class="mi">1</span>
<span class="k">def</span> <span class="nf">beta_0_update</span><span class="p">(</span><span class="n">X</span><span class="p">,</span> <span class="n">y</span><span class="p">,</span> <span class="n">phi_1</span><span class="p">):</span>
    <span class="n">m_1</span> <span class="o">=</span> <span class="n">phi_1</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span> <span class="c1"># treat as fixed
</span>    <span class="n">n</span> <span class="o">=</span> <span class="n">X</span><span class="p">.</span><span class="n">shape</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span>
    <span class="n">s_0_new</span> <span class="o">=</span> <span class="mi">1</span> <span class="o">/</span> <span class="p">((</span><span class="mi">1</span> <span class="o">/</span> <span class="n">TAU_0_SQUARED</span><span class="p">)</span> <span class="o">+</span> <span class="p">(</span><span class="n">n</span> <span class="o">/</span> <span class="n">SIGMA_SQUARED</span><span class="p">))</span> <span class="c1"># s_{0, *}^2
</span>    <span class="n">m_0_new</span> <span class="o">=</span> <span class="p">(</span><span class="n">s_0_new</span> <span class="o">/</span> <span class="n">SIGMA_SQUARED</span><span class="p">)</span> <span class="o">*</span> <span class="n">np</span><span class="p">.</span><span class="nf">sum</span><span class="p">(</span><span class="n">y</span> <span class="o">-</span> <span class="n">X</span> <span class="o">*</span> <span class="n">m_1</span><span class="p">)</span>
    <span class="nf">return </span><span class="p">(</span><span class="n">m_0_new</span><span class="p">,</span> <span class="n">s_0_new</span><span class="p">)</span>


<span class="k">def</span> <span class="nf">beta_1_update</span><span class="p">(</span><span class="n">X</span><span class="p">,</span> <span class="n">y</span><span class="p">,</span> <span class="n">phi_0</span><span class="p">):</span>
    <span class="n">m_0</span> <span class="o">=</span> <span class="n">phi_0</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span>
    <span class="n">s_1_new</span> <span class="o">=</span> <span class="mi">1</span> <span class="o">/</span> <span class="p">((</span><span class="mi">1</span> <span class="o">/</span> <span class="n">TAU_1_SQUARED</span><span class="p">)</span> <span class="o">+</span> <span class="p">(</span><span class="n">np</span><span class="p">.</span><span class="nf">sum</span><span class="p">(</span><span class="n">np</span><span class="p">.</span><span class="nf">pow</span><span class="p">(</span><span class="n">X</span><span class="p">,</span> <span class="mi">2</span><span class="p">))</span> <span class="o">/</span> <span class="n">SIGMA_SQUARED</span><span class="p">))</span> <span class="c1"># s_{1, *}^2
</span>    <span class="n">m_1_new</span> <span class="o">=</span> <span class="p">(</span><span class="n">s_1_new</span> <span class="o">/</span> <span class="n">SIGMA_SQUARED</span><span class="p">)</span> <span class="o">*</span> <span class="n">np</span><span class="p">.</span><span class="nf">sum</span><span class="p">(</span><span class="n">X</span> <span class="o">*</span> <span class="p">(</span><span class="n">y</span> <span class="o">-</span> <span class="n">m_0</span><span class="p">))</span>
    <span class="nf">return </span><span class="p">(</span><span class="n">m_1_new</span><span class="p">,</span> <span class="n">s_1_new</span><span class="p">)</span>
</code></pre></div></div> <p>From here writing the CAVI algorithm to learn $\phi$ is a piece of cake:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">run_cavi</span><span class="p">(</span><span class="n">X</span><span class="p">,</span> <span class="n">y</span><span class="p">):</span>
    <span class="n">phi_0</span><span class="p">,</span> <span class="n">phi_1</span> <span class="o">=</span> <span class="p">[</span><span class="mi">0</span><span class="p">,</span> <span class="mi">1</span><span class="p">],</span> <span class="p">[</span><span class="mi">0</span><span class="p">,</span> <span class="mi">1</span><span class="p">]</span>
    <span class="n">phi_0</span><span class="p">,</span> <span class="n">phi_1</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">array</span><span class="p">(</span><span class="n">phi_0</span><span class="p">),</span> <span class="n">np</span><span class="p">.</span><span class="nf">array</span><span class="p">(</span><span class="n">phi_1</span><span class="p">)</span>
    <span class="nf">while </span><span class="p">(</span><span class="bp">True</span><span class="p">):</span>
        <span class="n">phi_0_new</span> <span class="o">=</span> <span class="nf">beta_0_update</span><span class="p">(</span><span class="n">X</span><span class="p">,</span> <span class="n">y</span><span class="p">,</span> <span class="n">phi_1</span><span class="p">)</span>
        <span class="n">phi_1_new</span> <span class="o">=</span> <span class="nf">beta_1_update</span><span class="p">(</span><span class="n">X</span><span class="p">,</span> <span class="n">y</span><span class="p">,</span> <span class="n">phi_0_new</span><span class="p">)</span>
        <span class="nf">if </span><span class="p">(</span><span class="n">np</span><span class="p">.</span><span class="nf">sum</span><span class="p">(</span><span class="n">np</span><span class="p">.</span><span class="nf">abs</span><span class="p">(</span><span class="n">phi_0_new</span> <span class="o">-</span> <span class="n">phi_0</span><span class="p">))</span> <span class="o">&lt;</span> <span class="mf">1e-12</span><span class="p">):</span>
            <span class="k">break</span>
        <span class="n">phi_0</span> <span class="o">=</span> <span class="n">phi_0_new</span>
        <span class="n">phi_1</span> <span class="o">=</span> <span class="n">phi_1_new</span>
    <span class="k">return</span> <span class="n">phi_0</span><span class="p">,</span> <span class="n">phi_1</span>
</code></pre></div></div> <p>Similar to how we wrote <code class="language-plaintext highlighter-rouge">sample_from_blr_posterior(beta, X, y)</code> function to sample from our exact posterior, we’ll do the same to sample \(\beta\) from our CAVI posterior:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># evaluate q_{phi}(beta) by first learning phi
</span><span class="k">def</span> <span class="nf">sample_from_cavi_posterior</span><span class="p">(</span><span class="n">X</span><span class="p">,</span> <span class="n">y</span><span class="p">,</span> <span class="n">num_samples</span><span class="o">=</span><span class="mi">1000</span><span class="p">):</span>
  <span class="n">phi_0</span><span class="p">,</span> <span class="n">phi_1</span> <span class="o">=</span> <span class="nf">run_cavi</span><span class="p">(</span><span class="n">X</span><span class="p">,</span> <span class="n">y</span><span class="p">)</span>
  <span class="n">beta_0_samples</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="n">random</span><span class="p">.</span><span class="nf">normal</span><span class="p">(</span><span class="n">loc</span><span class="o">=</span><span class="n">phi_0</span><span class="p">[</span><span class="mi">0</span><span class="p">],</span> <span class="n">scale</span><span class="o">=</span><span class="n">np</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">(</span><span class="n">phi_0</span><span class="p">[</span><span class="mi">1</span><span class="p">]),</span> <span class="n">size</span><span class="o">=</span><span class="n">num_samples</span><span class="p">)</span>
  <span class="n">beta_1_samples</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="n">random</span><span class="p">.</span><span class="nf">normal</span><span class="p">(</span><span class="n">loc</span><span class="o">=</span><span class="n">phi_1</span><span class="p">[</span><span class="mi">0</span><span class="p">],</span> <span class="n">scale</span><span class="o">=</span><span class="n">np</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">(</span><span class="n">phi_1</span><span class="p">[</span><span class="mi">1</span><span class="p">]),</span> <span class="n">size</span><span class="o">=</span><span class="n">num_samples</span><span class="p">)</span>
  <span class="k">return</span> <span class="n">np</span><span class="p">.</span><span class="nf">column_stack</span><span class="p">((</span><span class="n">beta_0_samples</span><span class="p">,</span> <span class="n">beta_1_samples</span><span class="p">))</span>
</code></pre></div></div> <h4 id="step-three-comparing-predictive-distributions">step three: comparing predictive distributions</h4> <p>The hard part is behind us: we have now been able to learn the posterior \(\beta \mid D\) through two ways: (1) exact posterior derivation and (2) CAVI. Our goal now is to compare how (1) vs. (2) stack up in terms of forming predictive distributions. Recall that our predictive distribution for some fixed test point \(x\) can be given as</p> \[p\bigl(\hat y \mid D, x\bigr) = \int p\bigl(\hat y \mid \beta, x)\,p\bigl(\beta \mid D)\,\mathrm{d}\beta\] <p>and is typically approximated via Monte Carlo integration<sup id="fnref:mc-note"><a href="#fn:mc-note" class="footnote" rel="footnote" role="doc-noteref">3</a></sup>. As we’ll see, we use empirical confidence intervals instead to estimate the predictive distribution.</p> <p>We first generate $n = 1000$ data points using our <code class="language-plaintext highlighter-rouge">generate_data</code> function. We plot the data below along with its line of best fit (orange):</p> <div style="text-align: center;"> <img src="/assets/img/bnn-post/line-of-best-fit.png"/> </div> <p>We now present our concrete procedure to compare predictive distributions: for any fixed point \(x\) we sample \(K = 1000\) potential \(\beta\)s from our choice of posterior. Then<sup id="fnref:x-note"><a href="#fn:x-note" class="footnote" rel="footnote" role="doc-noteref">4</a></sup>, for each \(\beta_i\) we sample $\hat{y}$ using its conditional distribution \(\hat{y} \mid x, \beta_i \sim \mathcal{N}(x\beta_i, \sigma^2)\) where \(\sigma^2 = 1\). From these set of \(K\) sampled predictions, we can compute the predicted mean and form an empirical 95% confidence interval for the true \(\hat{y}\). This gives us the <em>predictive distribution confidence interval</em>.</p> <p>We visually show this procedure below for at \(x = 0\) by plotting the 95% predictive distribution confidence intervals computed from the exact BLR posterior (blue) and the CAVI-learned posterior (green).</p> <div style="text-align: center;"> <img src="/assets/img/bnn-post/blr-vs-cavi-fixed-point.png" style="max-width: 90%; height: auto;"/> </div> <p><br/></p> <p>We can see that the widths and placements of the predictive confidence intervals themselves are nearly identical, and both predicted means (the dots) are pretty close to the line of best fit. We now repeat this exact same procedure to compare predictive distribution confidence intervals across 1000 different choices of \(x\) from \([-3, 3]\), all in the same plot:</p> <div style="text-align: center;"> <img src="/assets/img/bnn-post/blr-vs-cavi-coverage.png" style="max-width: 90%; height: auto;"/> </div> <p>and … the predicted means and predictive confidence intervals are nearly identical again (at every point!), meaning that the CAVI algorithm has successfully modeled the true posterior. Truth be told, CAVI doesn’t always work this well. Many things were working in our favor here, which we list below:</p> <ol> <li>We assumed to know the correct value of our residual variance \(\sigma^2\), as opposed to estimating it.</li> <li>Our assumption that \(\beta_0\) and \(\beta_1\) are independently and normally distributed are 100% correct (by design, see our <code class="language-plaintext highlighter-rouge">generate_data</code> function.)</li> <li>We used a sufficiently large number of samples ($n = 1000$), which is always helpful.</li> <li>We used a sufficiently large number of parameter samples from the posterior (i.e. $K = 1000$) to compute our predictive distribution confidence intervals.</li> </ol> <p>For fun, we can imagine what would happen if we didn’t take #4 above for granted. How would the (empirical) confidence intervals for predictions look if we sampled less parameters from the posterior? We create the satisfying fun video to show just that for values of \(K\) from two to 1000:</p> <div style="text-align: center;"> <img src="/assets/img/bnn-post/predictive_convergence.gif" style="max-width: 90%; height: auto;"/> </div> <h4 id="code">code</h4> <p>I have put the code used for this blog post on this <a href="https://github.com/anish-lakkapragada/bnn-predictive-distributions">GitHub repo</a>. Most of the code shown in this blog post is located in <code class="language-plaintext highlighter-rouge">blr.py</code> and <code class="language-plaintext highlighter-rouge">cavi.py</code>.</p> <h4 id="resources">resources</h4> <p>Writing this piece required a lot of scavenging the web. Here’s my sources:</p> <ol> <li>Notation on BNNs largely follows this <a href="https://www.cs.toronto.edu/~duvenaud/distill_bayes_net/public/">piece</a> from the University of Toronto.</li> <li>For understanding Bayesian Linear Regression and deriving the exact posterior of \(\beta\) check out these <a href="https://www.cs.toronto.edu/~rgrosse/courses/csc411_f18/slides/lec19-slides.pdf">class slides</a> from University of Toronto’s CSC 411 and this <a href="https://medium.com/intuition/gentle-introduction-of-bayesian-linear-regression-c83da6b0d1f7">Medium article</a>.</li> <li>For understanding variational inference, see this review <a href="https://arxiv.org/pdf/1601.00670">paper</a> or these <a href="https://www.cs.princeton.edu/courses/archive/fall11/cos597C/lectures/variational-inference-i.pdf">notes</a> from Princeton’s COS597C (both from Dr. Blei!). Additionally, these <a href="https://www.cs.cmu.edu/~mgormley/courses/10418//slides/lecture17-cavi.pdf">slides</a> from CMU’s 10-418 explain CAVI in depth.</li> </ol> <p>If you find this piece helpful, please cite this blog post below:</p> <div class="language-bibtex highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">@article</span><span class="p">{</span><span class="nl">bnns-cavi-versus-blr</span><span class="p">,</span>
  <span class="na">title</span>   <span class="p">=</span> <span class="s">{What if we didn't approximate posteriors?}</span><span class="p">,</span>
  <span class="na">author</span>  <span class="p">=</span> <span class="s">{Lakkapragada, Anish}</span><span class="p">,</span>
  <span class="na">year</span>    <span class="p">=</span> <span class="s">{2025}</span><span class="p">,</span>
  <span class="na">month</span>   <span class="p">=</span> <span class="s">{May}</span><span class="p">,</span>
  <span class="na">url</span>     <span class="p">=</span> <span class="s">{https://anish.lakkapragada.com/blog/2025/bnn/}</span>
<span class="p">}</span>
</code></pre></div></div> <p><em>Thank you for reading this blog post. If you have any questions or noticed any errata, please <a href="mailto:anish.lakkapragada@yale.edu">email me</a>.</em></p> <div class="footnotes" role="doc-endnotes"> <ol> <li id="fn:kl-note"> <p>These expectations are over $\theta \sim q_{\phi}(\theta)$. <a href="#fnref:kl-note" class="reversefootnote" role="doc-backlink">&#8617;</a></p> </li> <li id="fn:cavi-note"> <p>In case you were wondering why we aren’t optimizing the ELBO with gradient ascent, keep in mind that the ELBO is likely a non-convex function. Furthermore, computing the ELBO (and then its gradient w.r.t parameters) is not a trivial task. <a href="#fnref:cavi-note" class="reversefootnote" role="doc-backlink">&#8617;</a></p> </li> <li id="fn:mc-note"> <p>Namely, one would sample $K$ samples \(\beta_0, \dots, \beta_K \overset{\text{i.i.d}}{\sim} \beta \mid D\) and then approximate \(p\bigl(\hat y \mid D, x\bigr)\) with \(p\bigl(\hat y \mid D, x\bigr) = \int p\bigl(\hat y \mid \beta, x)\,p\bigl(\beta \mid D)\,\mathrm{d}\beta \approx \frac{1}{K} \sum_{i = 1}^{K} p(\hat{y} \mid \beta_i, x)\). <a href="#fnref:mc-note" class="reversefootnote" role="doc-backlink">&#8617;</a></p> </li> <li id="fn:x-note"> <p>We glossed over a subtle detail here: our point $x$ must be represented as the two-dimensional vector \([1, x]\). <a href="#fnref:x-note" class="reversefootnote" role="doc-backlink">&#8617;</a></p> </li> </ol> </div>]]></content><author><name></name></author><category term="ml, statistics"/><category term="ml, variational-inference, bayesian-neural-networks"/><summary type="html"><![CDATA[a toy bayesian neural network with an exact $$\beta \mid D$$]]></summary></entry><entry><title type="html">new website!</title><link href="https://anish-lakkapragada.github.io/blog/2025/first-post/" rel="alternate" type="text/html" title="new website!"/><published>2025-03-17T16:40:16+00:00</published><updated>2025-03-17T16:40:16+00:00</updated><id>https://anish-lakkapragada.github.io/blog/2025/first-post</id><content type="html" xml:base="https://anish-lakkapragada.github.io/blog/2025/first-post/"><![CDATA[<p>This is my first blog post for this website, so needless to say (1) it is serving more as a test than anything and (2) it is hard not to be nervous that I will forget about this blog and never update it again. My previous website/blog/GitHub-Pages-Hosted-Thing was <em>Moments of Clarity</em> and has since moved to <a href="https://old-anish.lakkapragada.com">https://old-anish.lakkapragada.com</a>. I am excited to turn the page and create a more focused website for my college years.</p> <p>Specifically, I am hoping to use this space to post and host more carefully planned articles, notes, and pieces. Furthermore, I want this website to be more concentrated on my interests than my last one, which was terribly scattered. Those are my goals for myself, and I really hope I can satisfy them.</p> <p>Regardless, thank you for visiting!</p>]]></content><author><name></name></author><category term="misc"/><summary type="html"><![CDATA[never escaping jekyll :)]]></summary></entry></feed>